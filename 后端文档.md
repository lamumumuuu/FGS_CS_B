# 计算机协会管理系统 - 后端文档

## 一、项目概述

本项目是一个基于 Spring Boot 框架构建的计算机协会管理系统后端服务，采用仙侠主题的宗门管理模式，实现了完整的用户认证、权限管理（RBAC）、任务大厅、弟子管理等核心功能。

### 技术栈

| 技术 | 版本/说明 |
|------|----------|
| Java | 17+ |
| Spring Boot | 主框架 |
| Spring Security | 认证授权 |
| MyBatis-Plus | ORM 框架 |
| PostgreSQL | 数据库 |
| Redis | 缓存/登录锁定 |
| JWT | 令牌认证 |
| Liquibase | 数据库迁移（已禁用） |
| Swagger/OpenAPI | API 文档 |

## 二、项目结构说明

```
src/main/java/com/example/computerassociation/
├── annotation/                 # 自定义注解
│   ├── RequiresPermission.java      # 权限校验注解
│   └── RequiresRole.java             # 角色校验注解
├── aspect/                     # AOP 切面
│   ├── PermissionAspect.java         # 权限校验切面
│   └── RequestLogAspect.java         # 请求日志切面
├── common/                     # 公共类
│   ├── Result.java                   # 统一响应封装
│   └── UserContext.java              # 用户上下文（ThreadLocal）
├── config/                     # 配置类
│   ├── CorsConfig.java               # 跨域配置
│   ├── DataInitializer.java          # 数据初始化器
│   ├── MyBatisPlusConfig.java        # MyBatis-Plus 配置
│   ├── OpenApiConfig.java            # OpenAPI 文档配置
│   ├── PasswordEncoderConfig.java    # 密码编码器配置
│   ├── RedisConfig.java              # Redis 配置
│   └── SecurityConfig.java           # Spring Security 配置
├── controller/                 # 控制器层
│   ├── UserController.java           # 用户管理接口（登录/注册/个人信息，含灵石数据）
│   ├── TaskController.java           # 任务管理接口（任务大厅）
│   ├── SectController.java           # 宗门事务接口（峰管理/弟子名册）
│   ├── RbacController.java           # 权限管理接口（权限/角色CRUD）
│   ├── FinanceController.java        # 财务接口（灵石流水/总可支配灵石/峰级财务/分配调拨/调整日志）
│   ├── AnnouncementController.java   # 公告接口（宗门公告/本峰公告）
│   └── EventController.java          # 活动接口（宗门活动/本峰活动）
├── dto/                        # 数据传输对象
│   ├── LoginDTO.java                 # 登录请求
│   ├── RegisterDTO.java              # 注册请求
│   ├── CreateTaskDTO.java            # 创建任务请求
│   ├── RejectTaskDTO.java            # 驳回任务请求
│   ├── CreateAnnouncementDTO.java    # 创建公告请求（title/content/type/peakId）
│   └── CreateEventDTO.java           # 创建活动请求（name/description/location/startTime/endTime/peakId/type/maxParticipants）
├── entity/                     # 实体类
│   ├── User.java                     # 用户实体
│   ├── Task.java                     # 任务实体
│   ├── Disciple.java                 # 弟子实体
│   ├── Peak.java                     # 峰实体
│   ├── Role.java                     # 角色实体
│   ├── Permission.java               # 权限实体
│   ├── UserRole.java                 # 用户-角色关联
│   ├── RolePermission.java           # 角色-权限关联
│   ├── LingshiTransaction.java       # 灵石流水实体
│   ├── Announcement.java             # 公告实体
│   ├── Event.java                    # 活动实体
│   └── AuditLog.java                 # 审计日志实体
├── exception/                  # 异常处理
│   ├── BusinessException.java        # 业务异常
│   └── GlobalExceptionHandler.java   # 全局异常处理器
├── filter/                     # 过滤器
│   └── JwtAuthenticationFilter.java  # JWT 认证过滤器
├── mapper/                     # 数据访问层
│   ├── UserMapper.java
│   ├── TaskMapper.java
│   ├── DiscipleMapper.java
│   ├── PeakMapper.java
│   ├── RoleMapper.java
│   ├── PermissionMapper.java
│   ├── UserRoleMapper.java
│   ├── RolePermissionMapper.java
│   ├── LingshiTransactionMapper.java
│   ├── AnnouncementMapper.java
│   ├── EventMapper.java
│   └── AuditLogMapper.java
├── service/                    # 服务层
│   ├── UserService.java              # 用户服务接口
│   ├── TaskService.java              # 任务服务接口
│   ├── SectService.java              # 宗门服务接口（含添加/删除弟子级联操作）
│   ├── PeakService.java              # 峰服务接口
│   ├── RoleService.java              # 角色服务接口
│   ├── PermissionService.java        # 权限服务接口
│   ├── AuditLogService.java          # 审计日志服务接口
│   ├── LingshiTransactionService.java # 灵石流水服务接口
│   ├── AnnouncementService.java      # 公告服务接口
│   └── EventService.java             # 活动服务接口
│   └── impl/                         # 服务实现类
│       ├── UserServiceImpl.java
│       ├── TaskServiceImpl.java
│       ├── SectServiceImpl.java      # 弟子增删改查、用户账号级联
│       ├── PeakServiceImpl.java
│       ├── RoleServiceImpl.java
│       ├── PermissionServiceImpl.java
│       ├── LingshiTransactionServiceImpl.java # 灵石流水记录与查询
│       ├── AnnouncementServiceImpl.java
│       ├── EventServiceImpl.java
│       └── AuditLogServiceImpl.java
├── util/                       # 工具类
│   ├── JwtUtil.java                  # JWT 工具
│   └── RedisUtil.java                # Redis 工具
└── ComputerAssociationApplication.java  # 启动类
```

## 三、各模块详细说明

### 3.1 实体类（Entity）

#### User - 用户实体
映射数据库表 `users`，存储系统用户基本信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| username | String | 用户名，唯一 |
| password | String | 加密后的密码（BCrypt） |
| avatar | String | 头像路径 |
| role | Integer | 角色编号（旧字段，已弃用） |
| status | Integer | 状态（1正常/0禁用） |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |
| lastLoginTime | LocalDateTime | 最后登录时间 |

#### Task - 任务实体
映射数据库表 `tasks`，存储悬赏任务信息。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| title | String | 任务标题 |
| description | String | 任务描述 |
| difficulty | String | 难度等级（黑铁/青铜/白银/黄金） |
| status | String | 状态（审核中/等待中/讨伐中/已提交/已完成/已驳回） |
| reward | Integer | 悬赏灵石数量 |
| deadline | LocalDateTime | 截止日期 |
| publisherId | Long | 发布者ID |
| completerId | Long | 完成者ID |
| techRequirements | String | 技术要求 |
| peakId | Long | 所属峰ID |
| rejectReason | String | 驳回原因 |
| reviewedBy | Long | 审核人ID |
| reviewedAt | LocalDateTime | 审核时间 |
| submissionDescription | String | 提交成果描述（勇者提交悬赏时填写并持久化） |
| attachmentUrl | String | 提交成果附件链接（勇者提交悬赏时填写并持久化） |
| createdAt | LocalDateTime | 创建时间 |
| updatedAt | LocalDateTime | 更新时间 |

#### Disciple - 弟子实体
映射数据库表 `disciples`，存储宗门弟子信息，与用户表一对一关联。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| userId | Long | 关联 users.id |
| name | String | 弟子姓名 |
| studentId | String | 学号 |
| role | String | 宗门角色 |
| peak | String | 所属峰（不含"管理台"选项） |
| lingshi | Long | 灵石数量 |
| joinedAt | LocalDateTime | 加入时间 |
| updatedAt | LocalDateTime | 更新时间 |
| createdAt | LocalDateTime | 创建时间 |

#### LingshiTransaction - 灵石流水实体
映射数据库表 `lingshi_transactions`，记录所有灵石增减变动明细。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| discipleId | Long | 弟子ID |
| discipleName | String | 弟子姓名（冗余存储） |
| type | String | 变更类型（reward/adjust_in/adjust_out/allocate_in/peak_transfer_in/peak_transfer_out/peak_return_in/peak_return_out/allocate_to_member） |
| amount | Long | 变更金额（正数增加，负数减少） |
| balance | Long | 变更后余额 |
| operatorId | Long | 操作人ID |
| operatorName | String | 操作人姓名 |
| remark | String | 备注 |
| peakId | Long | 关联峰ID |
| peakName | String | 关联峰名称 |
| createdAt | LocalDateTime | 创建时间 |

#### Announcement - 公告实体
映射数据库表 `announcements`，存储宗门公告（含宗门公告与本峰公告）。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| title | String | 公告标题 |
| content | String | 公告内容 |
| publisherId | Long | 发布者ID |
| publisherName | String | 发布者名称（冗余存储） |
| peakId | Long | 关联峰ID（全局公告为null） |
| type | String | 公告类型（global/peak） |
| status | String | 状态（published/draft） |
| createdAt | LocalDateTime | 创建时间 |
| updatedAt | LocalDateTime | 更新时间 |

#### Event - 活动实体
映射数据库表 `events`，存储宗门活动（含全协会活动与本峰活动）。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| name | String | 活动名称 |
| description | String | 活动描述 |
| organizerId | Long | 组织者ID（即发布者用户ID） |
| organizerName | String | 组织者名称（使用发布者用户名 username 记录与显示） |
| peakId | Long | 关联峰ID |
| type | String | 活动类型（global/peak） |
| status | String | 状态（planned/ongoing/completed/cancelled），前端展示归为两类：进行中（planned+ongoing）/ 已结束（completed+cancelled） |
| startTime | LocalDateTime | 开始时间（精确到小时，格式 `YYYY-MM-DDTHH:00`） |
| endTime | LocalDateTime | 结束时间（精确到小时，格式 `YYYY-MM-DDTHH:00`） |
| location | String | 活动地点 |
| maxParticipants | Integer | 最大参与人数 |
| participantCount | Integer | 已参加人数（非数据库字段，查询时动态填充，对应 `event_participants` 表统计） |
| createdAt | LocalDateTime | 创建时间 |
| updatedAt | LocalDateTime | 更新时间 |

> **`participantCount` 非数据库字段说明**：`participantCount` 为瞬时字段，通过 `@TableField(exist = false)` 标注，不映射 `events` 表任何列。该字段在查询时动态填充：`getActiveEvents` / `getMyEvents` 遍历结果集时逐个调用 `eventMapper.countParticipants(eventId)` 统计 `event_participants` 表中对应活动的参与人数；`getEventById` 返回前同样填充。前端据此展示活动"已参加人数/最大人数"。

> **活动状态展示分类说明**：后端存储四种状态（`planned`、`ongoing`、`completed`、`cancelled`），前端展示时简化为两个类别：
> - **进行中**：包含 `planned`（已规划）和 `ongoing`（进行中）状态。进行中的活动在前端不显示状态标签。
> - **已结束**：包含 `completed`（已结束）和 `cancelled`（已取消）状态。已结束的活动在前端显示"已结束活动"标签。

> **时间精度说明**：所有活动时间（开始时间、结束时间）均精确到小时，不保留分钟，展示格式为 `YYYY-MM-DD HH:00`。前端使用自定义 `TimeWheelPicker` 组件（密码锁式滚轮选择器）替代原生 `datetime-local` 输入框进行时间选择，代码内时间格式为 `YYYY-MM-DDTHH:00`。

> **组织者信息说明**：活动组织者字段（`organizerName`）使用发布者的用户名（`username`）进行记录与展示，创建活动时自动从当前登录用户获取。

#### Peak - 峰实体
映射数据库表 `peaks`，存储宗门各峰信息，同时通过 `_TREASURY_` 特殊峰持久化"宗门总可支配灵石"。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| name | String | 峰名称，唯一 |
| description | String | 峰描述 |
| totalLingshi | Long | 累计灵石（历史累计值） |
| availableLingshi | Long | 可支配灵石（当前可用余额） |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

**峰分类**：
- **业务峰**：项目峰、算法峰、电路峰 — 参与灵石分配、调拨与统计
- **系统控制面板**：管理台 — 非业务峰，不出现在"所属峰"选择器，**受保护，不可删除**
- **财务特殊峰**：`_TREASURY_` — 宗门总可支配灵石的持久化容器，**受保护，不可删除**

**受保护峰**：
以下两个峰由系统初始化创建，对系统核心功能至关重要，禁止通过任何接口删除：
- `_TREASURY_`：财务持久化容器，所有灵石收支均通过该峰的 `available_lingshi` 字段进行持久化
- `管理台`：系统控制面板，非业务峰，仅用于系统管理后台标识

#### Role - 角色实体
映射数据库表 `roles`，存储系统角色定义。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| name | String | 角色标识（如 sect_master） |
| displayName | String | 显示名称（如 宗主） |
| description | String | 描述 |
| level | Integer | 角色层级（0最高） |
| isSystem | Integer | 是否系统内置 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### Permission - 权限实体
映射数据库表 `permissions`，存储细粒度权限。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| name | String | 权限标识（如 quest:view_all） |
| displayName | String | 显示名称 |
| module | String | 所属模块 |
| description | String | 描述 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

#### UserRole - 用户角色关联
映射数据库表 `user_roles`，实现用户与角色的多对多关系。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| userId | Long | 用户ID |
| roleId | Long | 角色ID |
| peakId | Long | 所属峰ID（可为空） |
| createTime | LocalDateTime | 创建时间 |

#### RolePermission - 角色权限关联
映射数据库表 `role_permissions`，实现角色与权限的多对多关系。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| roleId | Long | 角色ID |
| permissionId | Long | 权限ID |
| createTime | LocalDateTime | 创建时间 |

#### AuditLog - 审计日志实体
映射数据库表 `audit_logs`，记录所有敏感操作。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| operatorId | Long | 操作人ID |
| operatorName | String | 操作人名称 |
| operation | String | 操作描述 |
| module | String | 操作模块 |
| targetType | String | 操作目标类型 |
| targetId | Long | 操作目标ID |
| beforeData | String | 操作前数据（JSON） |
| afterData | String | 操作后数据（JSON） |
| ipAddress | String | 操作IP |
| userAgent | String | 客户端信息 |
| status | Integer | 操作结果（1成功/0失败） |
| errorMessage | String | 错误信息 |
| createTime | LocalDateTime | 操作时间 |

### 3.2 控制器层（Controller）

#### UserController - 用户管理控制器
路径前缀：`/api/user`

| 方法 | HTTP方法 | 路径 | 说明 | 认证 |
|------|----------|------|------|------|
| register | POST | /register | 用户注册 | 公开 |
| login | POST | /login | 用户登录 | 公开 |
| getUserInfo | GET | /me | 获取当前用户信息 | 需要JWT |
| logout | POST | /logout | 用户登出 | 需要JWT |
| refresh | POST | /refresh | 刷新Token | 公开 |

**核心逻辑**：
- 注册时自动分配外门弟子角色（outer_disciple）
- 注册时自动创建弟子记录
- 登录时验证密码并检查账户状态
- 登录失败触发Redis锁定机制（5次/15分钟）
- 登录成功返回Token + 用户完整权限信息

#### TaskController - 任务管理控制器
路径前缀：`/api/tasks`

| 方法 | HTTP方法 | 路径 | 说明 | 认证 |
|------|----------|------|------|------|
| getTasks | GET | / | 获取任务列表 | 需要JWT |
| getTaskById | GET | /{id} | 获取任务详情 | 需要JWT |
| createTask | POST | / | 创建任务 | 需要JWT + quest:create_draft |
| getPendingTasks | GET | /pending | 获取待审核任务 | 需要JWT + quest:review |
| approveTask | POST | /{id}/approve | 审核通过任务 | 需要JWT + quest:review |
| rejectTask | POST | /{id}/reject | 审核驳回任务 | 需要JWT + quest:review |
| submitTask | POST | /{id}/submit | 提交任务成果 | 需要JWT + quest:submit |
| completeTask | POST | /{id}/complete | 验收完成任务（发放奖励） | 需要JWT + quest:review_result |
| getMyTasks | GET | /my | 获取我发布的任务 | 需要JWT |
| getMyAcceptedTasks | GET | /accepted | 获取我接取的任务 | 需要JWT |
| getMyCompletedTasks | GET | /completed | 获取我完成的任务 | 需要JWT |

**核心逻辑**：
- 任务状态机：审核中 → 等待中 → 讨伐中 → 已提交 → 已完成（或在各阶段驳回/强制结项）
- 审核通过后任务对外展示
- 驳回时可附带驳回原因
- 完成者提交成果后，任务状态由"讨伐中"变为"已提交"，提交描述与附件链接持久化到数据库

> **成果提交与核查流程说明**：完成者接取任务后进入"讨伐中"，完成任务后调用 `POST /tasks/{id}/submit`（需 `quest:submit` 权限）填写提交描述与附件链接，任务进入"已提交"待验收状态。审核员在"成果核查"页面查看 `已提交` 状态任务的成果描述与附件，点击"验收通过"调用 `POST /tasks/{id}/complete`（需 `quest:review_result` 权限）→ 系统将 `task.reward` 灵石发放给完成者（写入 `disciples.lingshi` 并记录 `lingshi_transactions` 流水，type 为 `task_reward`）+ 任务状态变为"已完成"。

#### SectController - 宗门事务控制器
路径前缀：`/api/sect`

| 方法 | HTTP方法 | 路径 | 说明 | 权限 |
|------|----------|------|------|------|
| getCurrentUser | GET | /current-user | 获取当前用户宗门信息（disciples 表角色/峰） | 登录 |
| getAllDisciples | GET | /disciples | 获取所有弟子列表 | 登录 |
| getManagementDisciples | GET | /disciples/management | 获取管理层弟子 | 登录 |
| searchDisciples | GET | /disciples/search | 搜索弟子 | 登录 |
| getDiscipleById | GET | /disciples/{id} | 获取弟子详情 | 登录 |
| getAllPeaks | GET | /peaks | 获取所有峰列表（排除 `_TREASURY_` 特殊峰） | 登录 |
| addDisciple | POST | /disciples | 添加弟子（自动创建 User 账号，默认密码：123456） | member:update_role |
| updateDisciple | PUT | /disciples/{id} | 更新弟子信息 | member:update_role |
| deleteDisciple | DELETE | /disciples/{id} | 删除弟子 | member:remove |
| moveDisciplePeak | PUT | /disciples/{id}/move | 移动弟子门派 | peak:add_member |

> **前端使用变更**：`getCurrentUser` 返回 `disciples` 表中的宗门身份信息（角色中文名、峰名称）。个人主页（`/profile`）已迁移至前端 `PermissionContext` 全局状态（`roles` + `peakIds`），通过 `RbacController.getMyPermissions` 获取角色/峰关联，通过 `RbacController.getAllPeaks` 映射峰名称，与导航栏保持一致的全局数据源。此接口仍由宗门事务页（`useSectAffairs`）使用。

**核心逻辑**：
- 弟子角色变更时自动同步RBAC系统的用户角色
- 移动弟子时同步更新关联的峰信息
- 添加弟子时自动创建用户账号（默认密码：123456）
- 删除弟子时级联删除用户账号及所有关联数据（用户角色、灵石流水等）
- 所有敏感操作均记录审计日志

#### FinanceController - 财务管理控制器
路径前缀：`/api/finance`

| 方法 | HTTP方法 | 路径 | 说明 | 权限 |
|------|----------|------|------|------|
| getTotalDisposable | GET | /total-disposable | 获取宗门总可支配灵石 | finance:view_all |
| adjustTotalDisposable | POST | /adjust-total-disposable | 调整总可支配灵石 | finance:adjust_lingshi |
| getPeaksFinanceData | GET | /peaks | 获取所有峰的财务数据（峰列表） | finance:view_all |
| getPeakData | GET | /peak/{peakId} | 获取指定峰财务数据 | finance:view_own_peak |
| getPeakMembers | GET | /peak/{peakId}/members | 获取指定峰成员列表 | finance:view_own_peak |
| allocateToPeak | POST | /allocate-to-peak | 灵石分配（总→峰） | finance:adjust_lingshi |
| peakTransfer | POST | /peak-transfer | 峰间灵石调拨 | finance:adjust_lingshi |
| returnFromPeak | POST | /return-from-peak | 峰灵石归还至国库 | finance:adjust_lingshi |
| allocateToMember | POST | /allocate-to-member | 峰灵石分配到个人（峰→弟子） | finance:adjust_lingshi |
| getAdjustments | GET | /adjustments | 获取灵石调整日志（全部财务） | finance:view_all |
| getPeakAdjustments | GET | /peak-adjustments | 获取灵石调整日志（各峰财务） | finance:view_own_peak |
| getTransactions | GET | /transactions | 获取灵石流水列表 | finance:view_own_peak |
| getDiscipleTransactions | GET | /disciple/{id}/transactions | 获取指定弟子灵石流水 | finance:view_own_peak |
| adjustLingshi | POST | /adjust | 调整弟子灵石 | finance:adjust_lingshi |
| getBaseReward | GET | /base | 获取悬赏基准价格 | finance:view_own_peak |
| setBaseReward | PUT | /base | 设定悬赏基准价格 | finance:set_base |

**核心逻辑**：
- 所有财务接口排除 `_TREASURY_`（总可支配灵石容器）与 `管理台`（系统控制面板，非业务峰）
- 灵石分配操作：总可支配灵石 → 指定峰，原子事务同步更新
- 峰间调拨：源峰/目标峰均不能为 `管理台` 或 `_TREASURY_`
- 峰灵石归还至国库（return-from-peak）：
  - 校验峰存在且非受保护峰（`管理台` / `_TREASURY_`）
  - 校验归还金额 ≤ 峰当前可支配灵石
  - 扣减峰可支配灵石，通过 `_TREASURY_` 峰增加总可支配灵石
  - 记录两条流水：`peak_return_out`（峰支出）与 `peak_return_in`（国库收入）
- 峰灵石分配到个人（allocate-to-member）：
  - 校验峰存在、非受保护峰（`管理台` / `_TREASURY_`）、弟子存在、分配金额 > 0 且 ≤ 峰当前可支配灵石
  - 扣减峰 `available_lingshi`，通过 `discipleMapper.updateById(disciple)` 直接更新弟子 `lingshi`
  - 记录一条类型为 `allocate_to_member` 的灵石流水（关联峰ID与弟子ID）
  - 返回更新后的峰可支配灵石与弟子个人余额
  - 通过 `@Transactional` 保证扣减峰余额与增加弟子余额的原子性
  - **参数空安全检查**：对入参增加 null 安全校验，防止因参数为空引发 NPE 相关的 500 错误

> **避免权限检查冲突说明**：`allocateToMember` 与 `adjustLingshi` 改用 `discipleMapper.updateById(disciple)` 直接更新弟子信息，而不再通过 `sectService.updateDisciple(disciple)`。原因是 `sectService.updateDisciple` 内部会调用 `getDiscipleById`，而该方法包含峰级权限校验逻辑，财务操作场景下会触发 403/500 错误。直接使用 Mapper 可绕开权限检查冲突，确保财务操作的稳定性。
- 灵石调整日志支持按类型（全部财务）与峰ID（各峰财务）筛选，类型过滤列表包含：`reward`、`adjust_in`、`adjust_out`、`allocate_in`、`peak_transfer_in`、`peak_transfer_out`、`peak_return_in`、`peak_return_out`、`allocate_to_member`
- 所有调整操作均记录详细日志
- 调整总可支配灵石：增加或减少宗门公共可自由支配灵石，通过统一数据更新机制修改宗门公共灵石总量，并记录灵石流水，确保操作的原子性、一致性、隔离性和持久性；调整记录关联到 `_TREASURY_` 特殊峰，便于在灵石收支中追溯

> **峰财务数据独立性说明**：总财务（all）与峰财务（peak）使用独立的接口与数据源。总财务通过 `_TREASURY_` 峰的 `available_lingshi` 获取，峰财务通过各业务峰的 `available_lingshi` 获取，两者互不干扰。
>
> **峰财务显示条件说明**：峰财务相关接口仅在当前用户有关联峰（`peakIds.length > 0`）时才展示，无峰关联的用户不显示峰财务数据。

**核心方法说明**（私有方法）：
- `incrementTotalDisposable(long amount)` - 递增总可支配灵石（调整增加时调用）
  - 通过 `AtomicLong` 内存计数器原子递增，同步更新数据库 `_TREASURY_` 峰的 `available_lingshi` 字段
- `decrementTotalDisposable(long amount)` - 递减总可支配灵石（分配给峰/调整减少时调用）
  - 通过 `AtomicLong` 内存计数器原子递减，同步更新数据库 `_TREASURY_` 峰的 `available_lingshi` 字段
- `getCurrentTotalDisposable()` - 获取当前总可支配灵石
  - 优先读取内存 `AtomicLong` 计数器，确保高并发下的读取性能

**数据一致性保障**：
- **事务管理**：所有灵石操作（调整、分配、调拨、归还）通过 `@Transactional` 事务管理确保原子性、一致性、隔离性和持久性（ACID）
- **持久化容器**：`_TREASURY_` 特殊峰的 `available_lingshi` 字段作为总可支配灵石的唯一持久化容器，所有变动均同步写入该字段
- **内存缓存**：使用 `AtomicLong` 计数器在内存中缓存总可支配灵石值，通过 `incrementAndGet()` / `decrementAndGet()` 方法实现线程安全的原子操作
- **双写同步**：每次灵石变动同时更新内存计数器与数据库，保证内存与数据的最终一致性
- **流水追溯**：所有灵石操作均记录 `lingshi_transactions` 流水，关联 `_TREASURY_` 峰ID，便于审计与对账

#### AnnouncementController - 公告管理控制器
路径前缀：`/api/announcements`

| 方法 | HTTP方法 | 路径 | 说明 | 权限 |
|------|----------|------|------|------|
| getPublishedAnnouncements | GET | / | 获取已发布公告列表（根据用户权限过滤） | affair:view |
| getAnnouncementById | GET | /{id} | 获取公告详情 | affair:view |
| createAnnouncement | POST | / | 创建公告（全局公告需 affair:announce_global，本峰公告需 affair:announce_peak） | affair:announce_peak（最低门槛） |
| updateAnnouncement | PUT | /{id} | 更新公告内容 | affair:manage_own_peak |
| deleteAnnouncement | DELETE | /{id} | 删除公告 | affair:manage_own_peak |
| getMyAnnouncements | GET | /my | 获取当前用户发布的公告 | affair:view |

**核心逻辑**：
- 公告支持"宗门公告"（全局）与"本峰公告"（峰级）两种类型
- 发布公告需对应权限：全局公告需 `affair:announce_global`，本峰公告需 `affair:announce_peak`
- 公告全局可见，所有已登录用户均可访问
- 编辑/删除公告需为发布者本人或全局角色用户

> **公告权限与按钮过滤说明**：
> - **发布按钮**：仅拥有 `affair:announce_global` 或 `affair:announce_peak` 权限的用户可见"发布公告"按钮
> - **编辑/删除按钮**：仅拥有 `affair:announce_global` 或 `affair:announce_peak` 权限的用户可见"编辑/删除"按钮，无权限的用户无法看到按钮也无法访问对应功能
> - **后端权限校验**：`createAnnouncement` 方法内根据公告类型（global/peak）分别校验 `affair:announce_global` / `affair:announce_peak` 权限；`updateAnnouncement` / `deleteAnnouncement` 校验当前用户是否为公告发布者或全局角色用户
> - Controller 方法通过 `@RequiresPermission("affair:manage_own_peak")` 注解进行基础权限拦截，Service 层再进行细粒度的发布者/全局角色判断

#### EventController - 活动管理控制器
路径前缀：`/api/events`

| 方法 | HTTP方法 | 路径 | 说明 | 权限 |
|------|----------|------|------|------|
| getActiveEvents | GET | / | 获取活动列表（全局活动+本峰活动，根据用户权限过滤） | affair:view |
| getEventById | GET | /{id} | 获取活动详情 | affair:view |
| createEvent | POST | / | 创建活动 | affair:create_event |
| updateEvent | PUT | /{id} | 更新活动信息（仅非已结束活动可编辑） | affair:manage_own_peak |
| deleteEvent | DELETE | /{id} | 删除活动 | affair:manage_own_peak |
| joinEvent | POST | /{id}/join | 报名参加活动 | affair:view |
| endEvent | PUT | /{id}/end | 结束活动 | affair:manage_own_peak |
| getMyEvents | GET | /my | 获取当前用户组织的活动 | affair:view |

**核心逻辑**：
- 活动支持全协会活动（global）与本峰活动（peak）两种类型
- 活动状态流转：`planned` → `ongoing` → `completed`（或 `cancelled`），后端存储四种状态，前端展示归为"进行中"和"已结束"两类
- 创建活动需 `affair:create_event` 权限；管理活动（编辑/删除/结束）需 `affair:manage_all_events` 或 `affair:manage_own_peak` 权限
- 活动列表查询：全局用户可查看所有活动，非全局用户仅可查看全局活动与所属峰的活动
- 活动编辑仅对非已结束（`completed`/`cancelled`）的活动可用

> **活动状态展示分类与流转说明**：
> 后端存储四种活动状态（`planned`、`ongoing`、`completed`、`cancelled`），前端展示时简化为两个显示类别：
> - **进行中**：包含 `planned`（已规划）和 `ongoing`（进行中）。进行中的活动**不显示**状态标签。
> - **已结束**：包含 `completed`（已结束）和 `cancelled`（已取消）。已结束的活动显示"已结束活动"标签。
>
> 状态流转路径：
> - `planned` → `ongoing`：活动开始
> - `ongoing` → `completed`：通过 `PUT /events/{id}/end` 接口结束活动
> - `planned`/`ongoing` → `cancelled`：取消活动
>
> **活动编辑限制**：已结束（`completed`/`cancelled`）的活动不允许编辑，前端仅对非已结束活动显示"编辑"按钮。

> **活动报名接口说明**（`POST /events/{id}/join`）：
> - 用户通过该接口报名参加活动
> - 需登录且拥有 `affair:view` 权限（Controller 注解 `@RequiresPermission("affair:view")`）
> - **实现说明**：该接口已实现。`EventController.joinEvent` → `EventServiceImpl.joinEvent`（`@Transactional`），实现校验逻辑如下：
>   - 未登录（`userId == null`）→ 401
>   - 活动不存在 → 业务异常"活动不存在"
>   - 活动已结束/已取消（`completed`/`cancelled`）→ 业务异常"活动已结束，无法加入"
>   - 重复加入（`countUserParticipation` > 0）→ 业务异常"您已加入该活动"
>   - 人数已满（`maxParticipants` 非空且当前人数 ≥ 上限）→ 业务异常"活动参与人数已满"
>   - 校验通过后调用 `eventMapper.insertParticipant(eventId, userId)` 插入一条 `event_participants` 参与记录（`event_id`/`user_id` 由唯一约束 `uk_event_user` 保证不重复加入）
>   - 加入成功后后续查询可通过 `participantCount` 反映最新参与人数

> **活动结束接口说明**（`PUT /events/{id}/end`）：
> - 组织者或具备管理权限的用户通过该接口结束活动
> - 需拥有 `affair:manage_all_events` 或 `affair:manage_own_peak` 权限
> - 活动状态变更为 `completed`
> - ⚠️ **验证结论**：经核对当前后端代码，`EventController.java` 与 `EventService.java`/`EventServiceImpl.java` 中**尚未实现** `endEvent` 方法，该接口暂不存在于后端。前端三步向导第 3 步的"结束活动"按钮即调用此接口，当前会返回 404。完整核对见文末「十、前端公告页重构对齐与后端接口验证说明」。

> **活动权限与按钮过滤说明**：
> - **创建按钮**：仅拥有 `affair:create_event` 权限的用户可见"创建活动"按钮
> - **编辑/删除/结束按钮**：仅拥有 `affair:manage_all_events` 或 `affair:manage_own_peak` 权限的用户可见管理按钮，无权限的用户无法看到按钮也无法访问对应功能
> - **编辑按钮**：仅对非已结束（`completed`/`cancelled`）的活动显示
> - Controller 方法通过 `@RequiresPermission` 注解进行基础权限拦截（如 `affair:manage_own_peak`），Service 层再进行细粒度的组织者/全局角色/本峰权限判断

> **前端活动筛选变更说明**：前端活动筛选已从按状态筛选调整为按范围筛选，提供以下三个筛选选项：
> - **全部活动**：展示所有活动，不传额外筛选参数
> - **本峰活动**：前端过滤 `peakId` 在当前用户 `peakIds` 数组中的活动
> - **已结束**：前端过滤状态为 `completed` 的活动

> **创建活动参数说明**：
> - `maxParticipants` 为可选参数，为空/null 时表示不限人数。前端使用开关按钮控制是否设置人数上限。
> - `startTime` 精确到小时（默认当前时间的整点时刻），格式 `YYYY-MM-DDTHH:00`
> - `endTime` 可为空，空值表示无截止时间；非空时同样精确到小时
> - `organizerName` 自动设置为当前登录用户的用户名（`username`）

> **前端三步向导（PublishEvent）与后端 API 流程映射说明**：
> 前端将原统一的 `Publish.tsx` 发布弹窗拆分为 `PublishAnnouncement.tsx`（公告发布）与 `PublishEvent.tsx`（活动发布）两个独立组件。活动创建/编辑改用横向三步向导，三步收集的字段最终合并为一次 `POST /api/events`（创建）或 `PUT /api/events/{id}`（更新）请求，对应后端 `CreateEventDTO`：
> - **第 1 步·活动信息**：`name`（活动名称）、`description`（活动描述）、`location`（活动地点）、`type`（活动类型 global/peak）→ 对应 `CreateEventDTO.name/description/location/type`
> - **第 2 步·活动时间**：`startTime`/`endTime`，通过 `TimeWheelPicker` 滚轮选择器选择，精确到小时（`YYYY-MM-DDTHH:00`）→ 对应 `CreateEventDTO.startTime/endTime`
> - **第 3 步·其他设置**：`maxParticipants`（最大参与人数，开关控制是否设置上限）→ 对应 `CreateEventDTO.maxParticipants`；同步骤内"删除"按钮调用 `DELETE /api/events/{id}`，"结束"按钮调用 `PUT /api/events/{id}/end`（该接口后端尚未实现，见上方验证结论）
>
> 向导仅在最后一步提交时一次性调用后端创建/更新接口，三步之间不产生额外后端请求；`peakId`、`organizerId`/`organizerName` 由后端根据当前登录用户自动填充，不由前端显式传入。完整接口验证结论见文末「十、前端公告页重构对齐与后端接口验证说明」。

#### RbacController - 权限管理控制器
路径前缀：`/api/rbac`

| 方法 | HTTP方法 | 路径 | 说明 | 权限 |
|------|----------|------|------|------|
| getMyPermissions | GET | /my-permissions | 获取当前用户权限 | 登录 |
| getAllRoles | GET | /roles | 获取所有角色 | 登录 |
| getAllPeaks | GET | /peaks | 获取所有峰（排除 `_TREASURY_` 特殊峰） | 登录 |
| deletePeak | DELETE | /peaks/{id} | 删除指定峰（需峰下无成员，且受保护峰不可删除） | peak:create |
| getUserRoles | GET | /user/{userId}/roles | 获取用户角色 | member:view_all |
| updateUserRoles | PUT | /user/{userId}/roles | 更新用户角色 | member:update_role |
| getUserPermissions | GET | /user/{userId}/permissions | 获取用户权限 | member:view_all |

> **前端 PermissionContext 数据源**：前端全局权限上下文（`PermissionContext`）通过 `getMyPermissions` 获取当前用户的角色列表（含 `displayName`）和所属峰ID列表（`peakIds`），通过 `getAllPeaks` 获取峰列表用于 `peakIds → 峰名称` 映射。导航栏、个人主页、宗门事务等页面统一从 `PermissionContext` 读取角色和峰信息，确保全局数据同步。`PermissionAutoRefresh` 组件在窗口获焦、页面可见性恢复时自动调用 `refreshPermissions()` 刷新权限数据。

### 3.3 服务层实现（Service）

#### UserServiceImpl
继承 `ServiceImpl<UserMapper, User>`

**核心方法**：
- `register(RegisterDTO)` - 注册新用户
  - 参数校验（用户名/密码非空、密码长度≥6位）
  - 用户名唯一性检查
  - BCrypt加密密码
  - 分配默认外门弟子角色
  - 自动创建弟子记录
  - 记录审计日志

- `login(String username, String password)` - 用户登录
  - Redis登录锁定检查
  - 密码验证
  - 账户状态检查
  - 清除失败记录
  - 更新最后登录时间

- `registerWithRole(User user, String roleName, Long peakId)` - 带角色注册
  - 用于系统初始化创建管理员

#### TaskServiceImpl
继承 `ServiceImpl<TaskMapper, Task>`

**核心方法**：
- `createTask(Long userId, CreateTaskDTO dto)` - 创建任务
  - 设置初始状态为"审核中"
  - 填充发布者信息
  - 记录审计日志

- `getPublishedTasks(difficulty, status, keyword)` - 获取已发布任务
  - 多条件筛选（难度/状态/关键词）
  - 排除审核中的任务

> **全局访问双重保障判断**（`getPublishedTasks` / `getTaskDetail` / `getPendingTasks` / `canReviewTask` 共4个方法）：判断条件采用 `isGlobalRoleUser(userId) || hasPermission(userId, "quest:view_all")`。即 `isGlobalRoleUser()` 与 `quest:view_all` 权限二者命中其一即视为全局用户，避免因角色检测失败导致全局角色无法查看/审核所有任务。

- `approveTask(Long taskId, Long reviewerId)` - 审核通过
  - 状态变更：审核中 → 等待中
  - 记录审核人和时间

- `rejectTask(Long taskId, Long reviewerId, String reason)` - 审核驳回
  - 状态变更：审核中 → 已驳回
  - 保存驳回原因

- `submitTask(Long taskId, Long userId, String description, String attachmentUrl)` - 提交任务成果
  - 权限校验：需要 `quest:submit` 权限，且当前用户为任务接受者
  - 验证任务状态为"讨伐中"，提交描述不能为空
  - 将 `description` 和 `attachmentUrl` 持久化到任务记录
  - 状态变更：讨伐中 → 已提交
  - 记录审计日志

- `completeTask(Long taskId, Long reviewerId)` - 验收完成任务
  - 权限校验：需要 `quest:review_result` 权限
  - 验证任务状态为"已提交"
  - 发放奖励（灵石）给任务完成者：读取 `task.reward`，更新 `disciples.lingshi`，并记录 `lingshi_transactions` 流水（type 为 `task_reward`）
  - 状态变更：已提交 → 已完成
  - 记录审计日志

- `getMyCompletedTasks(Long userId)` - 获取我完成的任务
  - 按 `completer_id = userId` 且 `status = 已完成` 过滤，用于个人中心"我的征途"展示

#### SectServiceImpl
继承 `ServiceImpl<DiscipleMapper, Disciple>`

**核心方法**：
- `getAllDisciples()` - 获取所有弟子
- `getManagementDisciples()` - 获取管理层弟子
- `getDisciplesByPeak(String peak)` - 按峰获取弟子
- `searchDisciples(String keyword)` - 搜索弟子

> **全局访问双重保障判断**（`getAllDisciples` / `getManagementDisciples` / `searchDisciples` / `getDiscipleById` 共4个方法）：判断条件由原来的 `!permissionService.isGlobalRoleUser(userId)` 调整为 `!permissionService.isGlobalRoleUser(userId) && !permissionService.hasPermission(userId, "member:view_all")`。即 `isGlobalRoleUser()` 与 `member:view_all` 权限二者命中其一即视为全局用户，避免因角色检测失败导致 `grand_elder` 等全局角色无法查看全部弟子。
- `addDisciple(Disciple disciple)` - 添加弟子
  - **User 账号自动创建流程**：当 `disciple.userId` 为 null 时，系统自动创建 User 账号：
    1. 使用弟子姓名作为用户名（`username = disciple.getName()`）
    2. 使用默认密码 `"123456"`（BCrypt 加密后存储）
    3. 设置用户状态为正常（`status = 1`）
    4. 插入用户记录并获取生成的 userId
    5. 将新 userId 赋值给弟子记录（`disciple.setUserId(user.getId())`）
  - 创建 Disciple 记录并关联 User
  - 同步 RBAC 角色和峰关联（`syncUserRoleAndPeak`）
  - 记录审计日志
- `updateDisciple(Disciple disciple)` - 更新弟子信息
  - 同步更新RBAC角色

- `deleteDisciple(Long id)` - 删除弟子
  - 同步清除RBAC角色

- `moveDisciplePeak(Long discipleId, String newPeak)` - 移动弟子门派
  - 同步更新RBAC峰关联

#### PeakServiceImpl
继承 `ServiceImpl<PeakMapper, Peak>`

**核心方法**：
- `getAllPeaks()` - 获取所有峰列表
  - **`_TREASURY_` 过滤逻辑**：查询时通过 `queryWrapper.ne("name", "_TREASURY_")` 排除名为 `_TREASURY_` 的峰。`_TREASURY_` 是财务模块的持久化容器（存储宗门总可支配灵石），不应在宗门事务的峰列表中展示
  - **全局访问双重保障判断**：通过 `isGlobalRoleUser(userId) || hasPermission(userId, "member:view_all")` 判断用户是否具备全局访问权限。`isGlobalRoleUser()` 与 `member:view_all` 权限二者命中其一即视为全局用户，确保 `grand_elder` 等全局角色在角色检测失败时仍可查看所有峰
  - 全局用户：返回除 `_TREASURY_` 外的所有峰
  - 非全局用户：仅返回其权限范围内的峰（通过 `permissionService.getUserPeakIds()` 获取），同样排除 `_TREASURY_`

- `getPeakById(Long id)` - 按 ID 获取单个峰
- `getPeakByName(String name)` - 按名称获取峰
- `getPeaksByUserId(Long userId)` - 获取用户所属的峰列表（通过 `user_roles` 关联查询）
- `addPeak(String name, String description)` - 添加新峰
- `deletePeak(Long id)` - 删除峰
  - 前置检查：峰不存在则返回 false；峰下存在成员（`hasMembers`）则返回 false
  - 受保护峰 `_TREASURY_` 和 `管理台` 不应被删除（参见下方"受保护峰"说明）
  - 删除成功后返回 true

- `hasMembers(Long peakId)` - 检查峰下是否有弟子（通过 `peakMapper.countMembersByPeakId()` 查询）
- `updatePeak(Long id, String name, String description)` - 更新峰信息
  - 权限检查：需拥有 `peak:edit_any`（修改任意峰）或 `peak:edit_own`（仅修改本峰）权限

**受保护峰说明**：
- `_TREASURY_`：财务持久化容器，存储宗门总可支配灵石，绝不能删除
- `管理台`：系统控制面板，非业务峰，绝不能删除
- 这两个峰在系统初始化时创建，对系统核心功能至关重要，禁止通过任何接口删除

#### RoleServiceImpl
**核心方法**：
- `getAllRoles()` - 获取所有角色
- `getRolesByUserId(Long userId)` - 获取用户角色
- `getRoleByName(String name)` - 按名称获取角色
- `updateUserRolesWithCheck(operatorId, userId, roleIds, peakId)` - 更新用户角色（含权限校验）
  - 防止越权操作（不能修改等级高于自己的用户）

#### PermissionServiceImpl
权限查询服务实现类，提供权限、角色、峰 ID 的查询，以及基于内存列表的 hasPermission / hasRole 判断。所有查询均对 null 参数做安全处理，返回空集合或 false。

**核心方法**：
- `getPermissionsByUserId(Long userId)` - 获取用户权限列表
- `getPermissionNamesByUserId(Long userId)` - 获取用户权限名称列表
- `getRolesByUserId(Long userId)` - 获取用户角色列表
- `getUserRoleNames(Long userId)` - 获取用户角色名称列表
- `hasPermission(Long userId, String permissionName)` - 判断用户是否拥有指定权限
- `hasRole(Long userId, String roleName)` - 判断用户是否拥有指定角色
- `hasAnyRole(Long userId, String... roleNames)` - 判断用户是否拥有任一指定角色
- `getUserPeakIds(Long userId)` - 获取用户关联的峰ID列表（通过 `user_roles` 关联查询）
- `isGlobalRoleUser(Long userId)` - 判断是否为全局角色用户（详见下方说明）
- `canModifyRolePermissions(Long operatorId)` - 判断操作者是否可修改角色权限（角色等级 ≤ 1）

**`isGlobalRoleUser()` 全局角色用户判断（已修复）**：
判断用户是否为"全局角色用户"，决定其能否查看所有峰的数据。采用双重判断机制：
1. **方式1（原始逻辑）**：检查 `user_roles` 表中是否存在 `peak_id IS NULL` 的记录，若存在则视为全局用户
2. **方式2（新增逻辑）**：通过 `getUserRoleNames()` 获取用户的角色名称列表，检查是否包含任一全局角色：
   - `sect_master`（宗主）
   - `grand_elder`（大长老）
   - `supreme_elder`（太上长老）
   - `honor_elder`（荣誉长老）

> **修复背景**：原逻辑仅依赖 `peak_id IS NULL` 判断，导致当 `grand_elder` 等全局角色被分配到具体峰（`peak_id` 非 NULL）时，无法识别其全局身份，从而无法查看所有峰。新增方式2后，全局角色用户无论 `peak_id` 是否为空，均保留全局查看能力。只要任一方式命中即判定为全局用户。

#### LingshiTransactionServiceImpl
继承 `ServiceImpl<LingshiTransactionMapper, LingshiTransaction>`，负责灵石流水的记录与查询。

**核心方法**：
- `getTransactions(...)` - 获取灵石流水列表
  - **全局访问双重保障判断**：通过 `isGlobalRoleUser(userId) || hasPermission(userId, "finance:view_all")` 判断用户是否具备全局查看权限。`isGlobalRoleUser()` 与 `finance:view_all` 权限二者命中其一即视为全局用户，确保 `grand_elder` 等全局角色在角色检测失败时仍可查看全部财务流水。

#### AnnouncementServiceImpl
继承 `ServiceImpl<AnnouncementMapper, Announcement>`，负责公告的 CRUD 操作及权限控制。

**核心方法**：
- `getPublishedAnnouncements(Long userId)` - 获取已发布公告列表
  - 全局用户（`isGlobalRoleUser`）可查看所有公告（全局+各峰）
  - 非全局用户仅可查看全局公告与所属峰的公告
  - 结果按创建时间倒序排列

- `getAnnouncementById(Long id)` - 获取公告详情
  - 全局公告所有人可见
  - 峰级公告仅本峰成员和发布者可见，非全局用户无权查看非本峰公告时抛出 403

- `createAnnouncement(Long userId, CreateAnnouncementDTO dto)` - 创建公告
  - **权限校验**：全局公告需 `affair:announce_global` 权限，本峰公告需 `affair:announce_peak` 权限
  - 本峰公告未指定 `peakId` 时，自动取用户所属的第一个峰
  - `publisherName` 设置为发布者真实用户名（通过 `resolvePublisherName(publisherId)` 调用 `userService.getById` 解析 `username`，解析失败才回退为 `"用户" + publisherId`）
  - 状态默认为 `published`

- `updateAnnouncement(Long id, Long userId, CreateAnnouncementDTO dto)` - 更新公告
  - **权限校验**：仅公告发布者本人或全局角色用户可编辑
  - 非发布者且非全局角色用户抛出 403

- `deleteAnnouncement(Long id, Long userId)` - 删除公告
  - **权限校验**：仅公告发布者本人或全局角色用户可删除
  - 非发布者且非全局角色用户抛出 403

- `getMyAnnouncements(Long userId)` - 获取当前用户发布的公告列表

> **公告权限模型说明**：公告的发布、编辑、删除操作均受权限控制。前端通过 `PermissionContext` 中的权限列表判断是否显示"发布"、"编辑"、"删除"按钮。拥有 `affair:announce_global` 或 `affair:announce_peak` 权限的用户可见发布按钮；编辑/删除按钮还需用户为公告发布者或全局角色。

#### EventServiceImpl
继承 `ServiceImpl<EventMapper, Event>`，负责活动的 CRUD 操作及权限控制。

**核心方法**：
- `getActiveEvents(Long userId)` - 获取活动列表
  - 全局用户（`isGlobalRoleUser`）可查看所有活动（全局+各峰）
  - 非全局用户仅可查看全局活动与所属峰的活动
  - 结果按开始时间升序排列
  - 遍历结果集为每个事件填充 `participantCount`（调用 `eventMapper.countParticipants`）

- `getEventById(Long id)` - 获取活动详情
  - 全局活动所有人可见
  - 峰级活动仅本峰成员和组织者可见，非全局用户无权查看非本峰活动时抛出 403
  - 返回前填充 `participantCount`

- `createEvent(Long userId, CreateEventDTO dto)` - 创建活动
  - **权限校验**：需拥有 `affair:create_event` 权限
  - `organizerId` 设置为当前用户ID，`organizerName` 使用发布者用户名记录
  - 初始状态为 `planned`
  - 活动类型默认为 `peak`（本峰活动）

- `updateEvent(Long id, Long userId, CreateEventDTO dto)` - 更新活动
  - **权限校验**（满足任一即可）：
    1. 当前用户为活动组织者（`organizerId` 匹配）
    2. 拥有 `affair:manage_all_events` 权限
    3. 全局角色用户（`isGlobalRoleUser`）
    4. 拥有 `affair:manage_own_peak` 权限且活动所属峰在用户的 `peakIds` 范围内
  - 已结束（`completed`/`cancelled`）的活动前端不显示编辑按钮

- `deleteEvent(Long id, Long userId)` - 删除活动
  - 权限校验逻辑与 `updateEvent` 一致

- `getMyEvents(Long userId)` - 获取当前用户组织的活动列表（按创建时间倒序）
  - 返回前为每个事件填充 `participantCount`

- `joinEvent(Long eventId, Long userId)` - 用户加入活动（`@Transactional`）
  - 校验顺序：未登录 → 活动不存在 → 已结束/已取消（`completed`/`cancelled`）→ 重复加入 → 人数已满
  - 校验通过后调用 `eventMapper.insertParticipant` 插入一条 `event_participants` 参与记录
  - 同一用户对同一活动由唯一约束 `uk_event_user` 保证只能加入一次

> **EventService 接口说明**：`EventService` 接口新增声明 `void joinEvent(Long eventId, Long userId)`，由 `EventServiceImpl` 实现（见上）。`joinEvent` 无对应 `endEvent` 方法——`endEvent` 仍尚未实现。

> **活动权限模型说明**：活动的创建需 `affair:create_event` 权限；编辑/删除/结束需满足组织者身份或拥有 `affair:manage_all_events` / `affair:manage_own_peak` 权限。前端通过 `PermissionContext` 判断按钮可见性，所有发布、编辑、删除按钮均有权限过滤，无权限用户不可见按钮也无法访问功能。活动编辑仅对非已结束活动可用。

> **活动组织者信息说明**：活动创建时，`organizerId` 记录当前登录用户ID，`organizerName` 使用发布者的用户名（`username`）进行记录与展示，确保组织者信息的可追溯性。

### 3.4 权限系统实现

#### 权限校验注解
`@RequiresPermission(value = "quest:create_draft")` 用于Controller方法或类上。

#### 权限校验切面
`PermissionAspect` 实现对 `@RequiresPermission` 注解的AOP处理：
1. 从 `UserContext` 获取当前用户ID
2. 查询用户拥有的权限列表
3. 检查是否具备所需权限
4. 无权限时抛出 `BusinessException`

#### JWT认证过滤器
`JwtAuthenticationFilter` 继承 `OncePerRequestFilter`：
1. 从请求头提取 Bearer Token
2. 解析JWT获取用户名
3. 加载用户信息并验证Token有效性
4. 设置Spring Security认证上下文
5. 设置自定义UserContext（供Controller使用）
6. 请求完成后清理ThreadLocal

### 3.5 数据库初始化

`DatabaseAutoInitializer`（执行顺序 0）在应用启动时检测数据库是否存在，不存在则自动创建并执行初始化脚本；存在则校验表结构完整性，任一必需表缺失即重新执行初始化脚本。`DataInitializer`（执行顺序 1）紧随其后创建默认管理员账号。

#### 初始化脚本顺序
1. `Users.sql` - 创建用户表
2. `rbac_init.sql` - 创建RBAC相关表并初始化数据
3. `task_and_sect_init.sql` - 创建任务表、弟子表、公告表、活动表及活动参与表

#### 表结构完整性校验
`DatabaseAutoInitializer.checkTablesExist()` 会检测以下 13 张必需表，任一缺失即触发初始化脚本重建（脚本均为幂等 `CREATE TABLE IF NOT EXISTS` / `ON CONFLICT DO NOTHING`，重复执行安全）：
`users`、`peaks`、`roles`、`permissions`、`role_permissions`、`user_roles`、`disciples`、`tasks`、`events`、`event_participants`、`announcements`、`audit_logs`、`lingshi_transactions`

#### 初始化数据
- **峰**：管理台、项目峰、算法峰、电路峰、`_TREASURY_`（宗门总可支配灵石特殊峰，默认100000灵石）
- **角色**：7种系统角色
- **权限**：34条细粒度权限，分6个模块
- **角色-权限关联**：预配置的权限矩阵

#### 启动初始化
`DataInitializer` 在应用启动时自动执行：
- 检查管理员账号是否存在
- 不存在则创建默认管理员（admin/admin123）
- 自动分配宗主（sect_master）角色

#### PermissionMigrator 权限迁移器
`PermissionMigrator` 负责在系统升级时对历史 RBAC 数据进行迁移与对齐，确保与 `rbac_init.sql` 中的角色/权限定义保持一致。

**已修复项**：
1. **`peak_master` 角色引用修正**：将历史代码中引用的 `peak_master` 角色统一改为 `elder`。原因：`peak_master` 角色在 `rbac_init.sql` 中并不存在，正确的峰主角色标识为 `elder`。
2. **`grand_elder` 权限对齐**：调整 `grand_elder`（大长老）的权限集合，使其与 `rbac_init.sql` 中的定义保持一致——拥有除 `system:admin` 外的全部权限。
3. **`affair:manage_event` 重命名迁移**：将历史权限标识 `affair:manage_event` 迁移至 `affair:manage_all_events`，以匹配 `rbac_init.sql` 中新的权限命名。
4. **移除重复授权块**：删除了 `elder` 角色权限授予中重复的代码块，避免重复写入导致的数据冗余。

## 四、数据库表结构设计

### 4.1 用户表（users）

```sql
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    avatar VARCHAR(255),
    role INTEGER DEFAULT 0,
    status INTEGER DEFAULT 1,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login_time TIMESTAMP
);
```

### 4.2 峰表（peaks）

```sql
CREATE TABLE IF NOT EXISTS peaks (
    id SERIAL PRIMARY KEY,
    name VARCHAR(32) NOT NULL UNIQUE,
    description VARCHAR(255),
    total_lingshi BIGINT DEFAULT 0,         -- 累计灵石（历史累计值）
    available_lingshi BIGINT DEFAULT 0,     -- 可支配灵石（当前可用余额）
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.3 权限表（permissions）

```sql
CREATE TABLE IF NOT EXISTS permissions (
    id SERIAL PRIMARY KEY,
    name VARCHAR(64) NOT NULL UNIQUE,
    display_name VARCHAR(64) NOT NULL,
    module VARCHAR(32) NOT NULL,
    description VARCHAR(255),
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.4 角色表（roles）

```sql
CREATE TABLE IF NOT EXISTS roles (
    id SERIAL PRIMARY KEY,
    name VARCHAR(32) NOT NULL UNIQUE,
    display_name VARCHAR(32) NOT NULL,
    description VARCHAR(255),
    level SMALLINT NOT NULL,
    is_system SMALLINT DEFAULT 1,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.5 角色权限关联表（role_permissions）

```sql
CREATE TABLE IF NOT EXISTS role_permissions (
    id SERIAL PRIMARY KEY,
    role_id INT NOT NULL,
    permission_id INT NOT NULL,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE,
    FOREIGN KEY (permission_id) REFERENCES permissions(id) ON DELETE CASCADE,
    UNIQUE (role_id, permission_id)
);
```

### 4.6 用户角色关联表（user_roles）

```sql
CREATE TABLE IF NOT EXISTS user_roles (
    id SERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    role_id INT NOT NULL,
    peak_id INT DEFAULT NULL,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (role_id) REFERENCES roles(id) ON DELETE CASCADE,
    FOREIGN KEY (peak_id) REFERENCES peaks(id) ON DELETE SET NULL
);
```

### 4.7 弟子表（disciples）

```sql
CREATE TABLE IF NOT EXISTS disciples (
    id SERIAL PRIMARY KEY,
    user_id BIGINT,
    name VARCHAR(64) NOT NULL,
    student_id VARCHAR(32) DEFAULT '',
    role VARCHAR(32) NOT NULL DEFAULT 'outer_disciple',
    peak VARCHAR(32) DEFAULT '无',
    lingshi BIGINT DEFAULT 0,
    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.8 任务表（tasks）

```sql
CREATE TABLE IF NOT EXISTS tasks (
    id SERIAL PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    description TEXT NOT NULL,
    difficulty VARCHAR(16) NOT NULL DEFAULT '黑铁',
    status VARCHAR(16) NOT NULL DEFAULT '审核中',
    reward INTEGER NOT NULL DEFAULT 0,
    deadline TIMESTAMP,
    publisher_id BIGINT,
    completer_id BIGINT,
    tech_requirements TEXT,
    peak_id INTEGER,
    reject_reason TEXT,
    reviewed_by BIGINT,
    reviewed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.9 灵石流水表（lingshi_transactions）

```sql
CREATE TABLE IF NOT EXISTS lingshi_transactions (
    id SERIAL PRIMARY KEY,
    disciple_id BIGINT,
    disciple_name VARCHAR(64),
    type VARCHAR(32) NOT NULL,
    amount BIGINT NOT NULL,
    balance BIGINT,
    operator_id BIGINT,
    operator_name VARCHAR(64),
    remark TEXT,
    peak_id BIGINT,
    peak_name VARCHAR(32),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.10 公告表（announcements）

```sql
CREATE TABLE IF NOT EXISTS announcements (
    id SERIAL PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    publisher_id BIGINT,
    publisher_name VARCHAR(64),
    peak_id BIGINT,
    type VARCHAR(16) NOT NULL DEFAULT 'global',
    status VARCHAR(16) NOT NULL DEFAULT 'published',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.11 活动表（events）

```sql
CREATE TABLE IF NOT EXISTS events (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    organizer_id BIGINT,
    organizer_name VARCHAR(64),
    peak_id BIGINT,
    type VARCHAR(16) NOT NULL DEFAULT 'global',
    status VARCHAR(16) NOT NULL DEFAULT 'planned',
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    location VARCHAR(255),
    max_participants INTEGER DEFAULT 50,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.12 活动参与表（event_participants）

记录用户加入活动的情况，实现活动参与人数的统计与去重。该表在 `task_and_sect_init.sql` 中、`events` 表索引之后创建。

```sql
CREATE TABLE IF NOT EXISTS event_participants (
    id SERIAL PRIMARY KEY,
    event_id BIGINT NOT NULL,                 -- 活动ID
    user_id BIGINT NOT NULL,                  -- 参与的弟子用户ID
    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- 加入时间
    CONSTRAINT uk_event_user UNIQUE (event_id, user_id)  -- 同一用户对同一活动只能加入一次
);
CREATE INDEX idx_event_participants_event_id ON event_participants(event_id);
CREATE INDEX idx_event_participants_user_id ON event_participants(user_id);
```

> **说明**：
> - 唯一约束 `uk_event_user` 保证同一用户对同一活动只能加入一次，从数据库层面防止重复加入。
> - `event_id` / `user_id` 均建立索引，支撑 `countParticipants`（按 `event_id` 统计参与人数）与 `countUserParticipation`（按 `event_id` + `user_id` 判断是否已加入）的高效查询。
> - 该表不通过 MyBatis-Plus 实体映射，由 `EventMapper` 中的 `@Insert` / `@Select` 注解 SQL（`insertParticipant` / `countParticipants` / `countUserParticipation`）直接操作。

### 4.13 审计日志表（audit_logs）

```sql
CREATE TABLE IF NOT EXISTS audit_logs (
    id SERIAL PRIMARY KEY,
    operator_id BIGINT,
    operator_name VARCHAR(64),
    operation VARCHAR(128) NOT NULL,
    module VARCHAR(32),
    target_type VARCHAR(64),
    target_id BIGINT,
    before_data TEXT,
    after_data TEXT,
    ip_address VARCHAR(64),
    user_agent VARCHAR(512),
    status SMALLINT DEFAULT 1,
    error_message TEXT,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 五、RBAC权限模型

### 5.1 角色定义

| 角色标识 | 显示名称 | 等级 | 权限范围 |
|----------|----------|------|----------|
| sect_master | 宗主 | 0 | 全部权限 |
| grand_elder | 大长老 | 1 | 除系统管理外的所有权限 |
| supreme_elder | 太上长老 | 1 | 全局查看+审批+监督 |
| honor_elder | 荣誉长老 | 1 | 纯查阅权限 |
| elder | 长老 | 1 | 本峰管理权限 |
| inner_disciple | 内门弟子 | 2 | 本峰查看+起草+接取 |
| outer_disciple | 外门弟子 | 2 | 基础浏览+接取+提交 |

### 5.2 权限模块

| 模块 | 权限数量 | 说明 |
|------|----------|------|
| quest | 13 | 任务相关权限 |
| member | 6 | 成员管理权限 |
| affair | 6 | 宗门事务权限（公告/活动） |
| peak | 4 | 峰管理权限 |
| finance | 5 | 财务权限 |
| system | 1 | 系统管理权限 |

### 5.3 权限示例

```
quest:view_all           - 查看所有任务
quest:create_draft       - 发布任务草稿
quest:review             - 审核/验收任务
member:update_role       - 修改成员角色
member:remove            - 开除成员
peak:add_member          - 峰成员管理
finance:view_all         - 查看全协会财务
finance:view_own_peak    - 查看本峰财务
finance:adjust_lingshi   - 调整灵石
finance:set_base         - 设定悬赏基准
system:admin             - 系统管理

# affair 模块（宗门事务 - 公告与活动）
affair:view              - 查看宗门事务（查看公告和活动）
affair:announce_global   - 发布宗门公告（全协会公告）
affair:announce_peak     - 发布本峰公告
affair:create_event      - 创建活动
affair:manage_all_events - 管理所有活动
affair:manage_own_peak   - 管理本峰活动（含公告编辑/删除、活动编辑/删除/结束）
```

> **affair 模块权限与按钮过滤对应关系**：
> - `affair:view`：查看公告和活动列表/详情（所有登录用户的基础权限）
> - `affair:announce_global`：可见"发布宗门公告"按钮，可发布全局公告
> - `affair:announce_peak`：可见"发布本峰公告"按钮，可发布本峰公告
> - `affair:create_event`：可见"创建活动"按钮
> - `affair:manage_all_events`：可见所有活动的"编辑/删除/结束"按钮
> - `affair:manage_own_peak`：可见本峰活动的"编辑/删除/结束"按钮，以及公告的"编辑/删除"按钮
>
> 公告编辑/删除还需满足：当前用户为公告发布者或全局角色用户。
> 活动编辑/删除/结束还需满足：当前用户为活动组织者，或拥有 `affair:manage_all_events` / `affair:manage_own_peak` 权限。
> 活动编辑仅对非已结束（`completed`/`cancelled`）活动可用。

## 六、文件间的上下承接关系

### 6.1 请求处理流程

```
HTTP Request
    ↓
CORS Filter (CorsConfig)
    ↓
JWT Filter (JwtAuthenticationFilter)
    ├── 提取Bearer Token
    ├── 解析JWT
    ├── 验证用户
    └── 设置UserContext
    ↓
Spring Security Authentication
    ↓
PermissionAspect (AOP)
    ├── 检查@RequiresPermission注解
    ├── 查询用户权限
    └── 权限验证
    ↓
Controller
    ├── 参数校验 (@Valid)
    ├── 调用Service
    └── 返回Result
    ↓
Service
    ├── 业务逻辑处理
    ├── 调用Mapper
    ├── 审计日志记录
    └── 事务管理
    ↓
Mapper (MyBatis-Plus)
    └── 数据库操作
    ↓
PostgreSQL
```

### 6.2 模块依赖关系

```
Controller → Service → Mapper → Database
     ↓           ↓
    DTO      Entity/VO
     ↓           ↓
   Aspect    PermissionService
     ↓           ↓
    Filter   UserRoleMapper
     ↓
  UserContext (ThreadLocal)
```

### 6.3 核心数据流

```
用户注册流程:
UserController.register → UserServiceImpl.register
    ├── 密码加密
    ├── 创建用户记录
    ├── 分配默认角色
    ├── 创建弟子记录
    └── 记录审计日志

任务发布流程:
TaskController.createTask → TaskServiceImpl.createTask
    ├── 权限校验
    ├── 创建任务记录（状态：审核中）
    └── 记录审计日志

成果核查流程:
TaskController.completeTask → TaskServiceImpl.completeTask
    ├── 权限校验（quest:review_result）
    ├── 验证任务状态为"已提交"
    ├── issueReward：发放 task.reward 灵石给完成者（更新 disciples.lingshi + 记录 lingshi_transactions 流水）
    ├── 更新任务状态为"已完成"
    └── 记录审计日志

弟子管理流程:
SectController.* → SectServiceImpl.*
    ├── CRUD操作
    ├── 同步RBAC角色
    └── 记录审计日志

公告发布流程:
AnnouncementController.createAnnouncement → AnnouncementServiceImpl.createAnnouncement
    ├── 权限校验（全局公告需 affair:announce_global / 本峰公告需 affair:announce_peak）
    ├── 设置 publisherId（当前用户ID）、publisherName（发布者用户名）
    ├── 本峰公告自动填充用户所属峰ID（未指定时）
    ├── 创建公告记录（状态：published）
    └── 记录操作日志

公告编辑/删除流程:
AnnouncementController.updateAnnouncement/deleteAnnouncement → AnnouncementServiceImpl.*
    ├── 权限校验（@RequiresPermission("affair:manage_own_peak") 基础拦截）
    ├── 细粒度校验：发布者本人 或 全局角色用户
    └── 更新/删除公告记录

活动创建流程:
EventController.createEvent → EventServiceImpl.createEvent
    ├── 权限校验（affair:create_event）
    ├── 设置 organizerId（当前用户ID）、organizerName（发布者用户名）
    ├── 设置初始状态为 planned
    ├── 时间精确到小时（YYYY-MM-DDTHH:00）
    └── 创建活动记录

活动管理流程（编辑/删除/结束）:
EventController.updateEvent/deleteEvent/endEvent → EventServiceImpl.*
    ├── 权限校验（@RequiresPermission("affair:manage_own_peak") 基础拦截）
    ├── 细粒度校验：组织者本人 或 affair:manage_all_events 或 全局角色 或 本峰管理权限
    ├── 编辑仅对非已结束（completed/cancelled）活动可用
    └── 结束活动时状态变更为 completed

活动报名流程:
EventController.joinEvent → EventServiceImpl.joinEvent
    ├── 权限校验（affair:view）
    ├── 校验未登录 / 活动不存在 / 已结束(cancelled) / 重复加入 / 人数已满
    └── 记录用户活动参与关系（event_participants 表，insertParticipant）

> ⚠️ 上述"活动管理流程"中的 `endEvent` 尚未实现，当前后端 `EventController`/`EventService` 未声明对应方法，详见「十、前端公告页重构对齐与后端接口验证说明」。`joinEvent` 已实现（见上方"活动报名流程"）。

活动列表查询流程:
EventController.getActiveEvents → EventServiceImpl.getActiveEvents
    ├── 获取全局活动（peak_id IS NULL）
    ├── 全局用户：获取所有峰的活动
    ├── 非全局用户：仅获取所属峰的活动（通过 getUserPeakIds 过滤）
    └── 按开始时间排序返回

前端权限数据获取流程（个人主页/导航栏统一数据源）:
RbacController.getMyPermissions → PermissionServiceImpl.getUserPermissions
    ├── 查询 user_roles 表（角色 + peak_id 关联）
    ├── 查询 role_permissions 表（权限列表）
    └── 返回 roles（含 displayName）+ peakIds + permissions
RbacController.getAllPeaks → PeakServiceImpl.getAllPeaks
    └── 返回峰列表（排除 _TREASURY_），前端用于 peakIds → 峰名称映射
```

## 七、系统核心功能描述

### 7.1 用户认证系统
- 支持注册、登录、登出、Token刷新
- BCrypt密码加密
- JWT无状态认证
- Redis登录锁定机制
- 支持多角色/多峰权限

### 7.2 任务大厅系统
- 任务发布（需审核）
- 任务审核（通过/驳回）
- 任务列表浏览（多条件筛选）
- 任务详情查看
- 任务状态机管理

### 7.3 宗门管理系统
- 弟子CRUD管理
- 峰信息管理
- 弟子角色管理
- 门派迁移
- 权限同步
- 添加弟子自动创建用户账号（默认密码123456）
- 删除弟子级联删除用户账号及关联数据

### 7.4 公告与活动系统

#### 7.4.1 系统架构概述
公告与活动系统是宗门事务模块的核心组成部分，提供信息发布与活动管理能力。系统采用前后端分离架构，后端提供 RESTful API，前端通过 `PermissionContext` 全局权限上下文控制按钮可见性与功能访问。

**核心组件**：
- **AnnouncementController / AnnouncementServiceImpl**：公告的发布、查询、编辑、删除
- **EventController / EventServiceImpl**：活动的创建、查询、编辑、删除、报名、结束
- **PermissionContext（前端）**：全局权限上下文，控制所有发布/编辑/删除按钮的可见性
- **TimeWheelPicker（前端）**：自定义密码锁式滚轮时间选择器，用于活动时间选择
- **PublishAnnouncement / PublishEvent（前端）**：原统一的 `Publish.tsx` 发布弹窗已拆分为 `PublishAnnouncement.tsx`（公告发布）与 `PublishEvent.tsx`（活动发布，采用三步向导）两个独立组件；前端 `index.ts` barrel 文件已删除，所有导入改为直接文件路径。此为前端架构调整，**不涉及后端接口变更**，公告/活动仍分别调用原有 `POST/PUT/DELETE /api/announcements`、`POST/PUT/DELETE /api/events`。

#### 7.4.2 公告子系统
- 宗门公告发布（全局可见，需 `affair:announce_global` 权限）
- 本峰公告发布（峰级可见，需 `affair:announce_peak` 权限）
- 公告编辑/删除（需为发布者本人或全局角色用户）
- 公告全局可见，所有已登录用户均可浏览
- 前端发布/编辑/删除按钮均有权限过滤，无权限用户不可见按钮

#### 7.4.3 活动子系统
- 全协会活动创建与管理（global 类型）
- 本峰活动创建与管理（peak 类型）
- 活动报名参加（`POST /events/{id}/join`）—— 已实现，需 `affair:view` 权限；校验未登录/活动不存在/已结束/重复加入/人数已满后写入 `event_participants` 参与记录
- 活动结束（`PUT /events/{id}/end`）—— ⚠️ 后端尚未实现（前端三步向导第 3 步"结束活动"按钮调用此接口），详见文末验证说明
- 活动组织者信息使用发布者用户名（`username`）记录与展示（`EventServiceImpl` 通过 `resolveOrganizerName(organizerId)` 调用 `userService.getById` 解析真实用户名，解析失败时才回退为 `"用户" + userId`）
- 活动已参加人数（`participantCount`）—— 非数据库字段，查询时基于 `event_participants` 表动态统计，前端用于展示"已参加人数/最大人数"

**活动状态机与展示分类**：
- 后端存储四种状态：`planned`（已规划）→ `ongoing`（进行中）→ `completed`（已结束）/ `cancelled`（已取消）
- 前端展示简化为两个类别：
  - **进行中**（`planned` + `ongoing`）：不显示状态标签
  - **已结束**（`completed` + `cancelled`）：显示"已结束活动"标签

**活动时间处理**：
- 所有活动时间精确到小时，不保留分钟
- 展示格式：`YYYY-MM-DD HH:00`
- 代码内格式：`YYYY-MM-DDTHH:00`
- 前端使用 `TimeWheelPicker` 组件（密码锁式滚轮选择器）替代原生 `datetime-local` 输入框

**活动权限控制**：
- 创建活动：需 `affair:create_event` 权限
- 管理（编辑/删除/结束）：需为组织者本人，或拥有 `affair:manage_all_events` / `affair:manage_own_peak` 权限
- 活动编辑仅对非已结束（`completed`/`cancelled`）活动可用
- 所有发布/编辑/删除按钮均有权限过滤，无权限用户不可见按钮也无法访问功能

### 7.5 财务管理系统
- 宗门总可支配灵石管理（通过 `_TREASURY_` 特殊峰持久化）
- 峰级灵石独立核算
- 灵石分配（总→峰）
- 峰间灵石调拨
- 峰灵石归还至国库（峰→总，支持单峰归还）
- 峰灵石分配到个人（峰→弟子，记录 `allocate_to_member` 流水）
- 灵石调整日志（全部财务 / 各峰财务分类，含 `peak_return_in` / `peak_return_out` / `allocate_to_member` 类型）
- 悬赏基准价格设定

> **峰财务数据独立性**：总财务（all）与峰财务（peak）使用独立的接口与数据源，互不干扰。
>
> **峰财务显示条件**：峰财务相关功能仅在用户有关联峰（`peakIds.length > 0`）时展示。

**数据一致性保障机制**：
- **事务原子性**：所有灵石操作（调整、分配、调拨、归还）通过 Spring `@Transactional` 声明式事务管理，确保多步操作要么全部成功，要么全部回滚
- **持久化容器**：`_TREASURY_` 特殊峰的 `available_lingshi` 字段作为总可支配灵石的唯一持久化容器，任何灵石变动均同步写入该字段
- **内存原子缓存**：使用 `AtomicLong` 计数器在内存中缓存总可支配灵石值，通过 `incrementAndGet()` / `decrementAndGet()` 方法实现线程安全的原子递增与递减，避免高并发场景下的数据竞争
- **双写同步策略**：每次灵石变动采用"内存先更新 + 数据库同步写入"的双写策略，保证内存缓存与数据库的最终一致性
- **流水审计追溯**：所有灵石操作均写入 `lingshi_transactions` 流水表，关联 `_TREASURY_` 峰ID与操作类型，便于审计核对与异常恢复
- **启动初始化**：应用启动时 `DataInitializer` 从数据库加载 `_TREASURY_` 峰的 `available_lingshi` 值，初始化内存 `AtomicLong` 计数器，确保内存与数据库的初始一致性

### 7.6 RBAC权限系统
- 7种固定角色
- 32条细粒度权限
- 基于注解的权限控制
- AOP切面自动校验
- 越权操作保护
- 前端通过 `PermissionContext` 统一管理用户角色（`roles`）、所属峰ID（`peakIds`）和权限列表（`permissions`），导航栏、个人主页、宗门事务等页面共享同一数据源，`PermissionAutoRefresh` 在窗口获焦时自动刷新，确保全局数据同步

### 7.5 审计日志系统
- 所有敏感操作自动记录
- 操作前后数据对比
- 操作人、IP、时间追踪
- 支持审计查询

## 八、配置说明

### 8.1 应用配置（application.yml）

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: never
  liquibase:
    enabled: false

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
```

### 8.2 环境变量

| 变量 | 说明 | 必需 |
|------|------|------|
| DB_HOST | 数据库主机 | 是 |
| DB_PORT | 数据库端口 | 是 |
| DB_NAME | 数据库名 | 是 |
| DB_USERNAME | 数据库用户名 | 是 |
| DB_PASSWORD | 数据库密码 | 是 |
| JWT_SECRET | JWT签名密钥 | 是 |
| JWT_EXPIRATION | Token有效期(秒) | 否，默认86400 |
| REDIS_HOST | Redis主机 | 否 |
| REDIS_PORT | Redis端口 | 否 |

### 8.3 启动要求
1. PostgreSQL 13+ 数据库
2. Redis 6+（用于登录锁定）
3. 执行SQL初始化脚本
4. 配置环境变量
5. Maven构建并运行

## 九、注意事项

1. **数据库初始化顺序**：必须按 Users.sql → rbac_init.sql → task_and_sect_init.sql 顺序执行
2. **首次启动**：系统会自动创建admin管理员账号（密码：admin123），首次登录后请修改
3. **角色系统**：删除RBAC配置中的角色和权限会影响系统功能
4. **Redis依赖**：Redis不可用时登录锁定功能会失效
5. **JWT密钥**：生产环境必须修改JWT_SECRET为随机密钥

## 十、前端公告页重构对齐与后端接口验证说明

> 本节为前端公告页重构后的后端文档对齐与接口核对记录。核对基准为 `src/main/java/com/example/computerassociation/` 下的 `AnnouncementController.java`、`EventController.java`、`EventService.java`、`EventServiceImpl.java`、`RbacController.java`、`CreateAnnouncementDTO.java`、`CreateEventDTO.java`。

### 10.1 前端架构变更对齐（不涉及后端接口变更）

| 前端变更 | 说明 | 后端影响 |
|----------|------|----------|
| `Publish.tsx` 拆分 | 拆分为 `PublishAnnouncement.tsx`（公告发布）与 `PublishEvent.tsx`（活动发布）两个独立组件 | 无，仍调用原有 `POST/PUT/DELETE /api/announcements`、`POST/PUT/DELETE /api/events` |
| `PublishEvent` 三步向导 | 活动创建/编辑改为横向三步：①活动信息 ②活动时间（TimeWheelPicker，小时精度）③其他设置（最大参与人数）+ 删除/结束按钮 | 无，三步字段合并后一次性调用 `POST /api/events` 或 `PUT /api/events/{id}`，对应 `CreateEventDTO` |
| `index.ts` barrel 文件删除 | 所有导入改为直接文件路径 | 无，纯前端模块组织调整 |

前端 `useAnnouncement` Hook 统一调用上述接口并管理本地状态，所有数据均来自后端数据库实时查询，前端无硬编码数据。

### 10.2 三步向导字段与后端 `CreateEventDTO` 映射

| 向导步骤 | 前端字段 | 后端 `CreateEventDTO` 字段 | 落库字段（events 表） |
|----------|----------|---------------------------|----------------------|
| 第 1 步·活动信息 | name / description / location / type | name / description / location / type | name / description / location / type |
| 第 2 步·活动时间 | startTime / endTime（TimeWheelPicker，`YYYY-MM-DDTHH:00`） | startTime / endTime | start_time / end_time |
| 第 3 步·其他设置 | maxParticipants（开关控制是否设置上限） | maxParticipants | max_participants |
| 后端自动填充 | —（不由前端传入） | peakId / organizerId / organizerName | peak_id / organizer_id / organizer_name |

> 向导仅在最后一步提交时发起一次创建/更新请求，三步之间不产生额外后端调用。

### 10.3 后端接口验证结论（逐条核对）

| 前端调用接口 | 后端 Controller 方法 | 实际是否存在 | 结论 |
|--------------|---------------------|--------------|------|
| `GET /api/announcements` | `AnnouncementController.getPublishedAnnouncements` | 是 | 匹配 |
| `POST /api/announcements` | `AnnouncementController.createAnnouncement` | 是 | 匹配 |
| `PUT /api/announcements/{id}` | `AnnouncementController.updateAnnouncement` | 是 | 匹配 |
| `DELETE /api/announcements/{id}` | `AnnouncementController.deleteAnnouncement` | 是 | 匹配 |
| `GET /api/events` | `EventController.getActiveEvents` | 是 | 匹配 |
| `POST /api/events` | `EventController.createEvent` | 是 | 匹配 |
| `PUT /api/events/{id}` | `EventController.updateEvent` | 是 | 匹配 |
| `DELETE /api/events/{id}` | `EventController.deleteEvent` | 是 | 匹配 |
| `POST /api/events/{id}/join` | `EventController.joinEvent` | **是** | 匹配（已实现，`@RequiresPermission("affair:view")`，写入 `event_participants` 表） |
| `PUT /api/events/{id}/end` | `EventController.endEvent` | **否** | ⚠️ 文档已记录但后端未实现（前端三步向导第 3 步"结束活动"按钮调用此接口） |
| `GET /api/rbac/peaks` | `RbacController.getAllPeaks` | 是 | 匹配 |

**待处理项**：
1. **`PUT /api/events/{id}/end` 后端缺失**：当前 `EventController` 已暴露 7 个端点（`GET /`、`GET /{id}`、`POST /`、`PUT /{id}`、`DELETE /{id}`、`GET /my`、`POST /{id}/join`），`EventService` 接口已声明 `joinEvent` 但尚未声明 `endEvent`。前端三步向导第 3 步的"结束活动"按钮会调用 `PUT /api/events/{id}/end`，当前将返回 404。建议在后端补充实现 `endEvent`（含对应 Service 方法与权限注解），或在前端暂时隐藏该按钮，待后端就绪后再启用。
2. **`organizerName` 字段与文档一致（已修复）**：此前多处文档称 `organizerName` 使用发布者用户名（`username`），但 `EventServiceImpl.createEvent` 曾存储为 `"用户" + userId`。现已通过新增 `resolveOrganizerName(organizerId)` 方法（内部调用 `userService.getById` 解析真实 `username`，解析失败才回退 `"用户" + userId`）统一修复创建与查询逻辑，组织者名称现与文档描述一致。公告发布者名称同理通过 `userService` 解析真实用户名。

### 10.4 权限要求核对结论

| 操作 | 文档记录权限 | 实际 Controller 注解 / Service 校验 | 结论 |
|------|--------------|-------------------------------------|------|
| 查看公告/活动 | `affair:view` | `@RequiresPermission("affair:view")` | 匹配 |
| 创建公告 | 全局 `affair:announce_global` / 本峰 `affair:announce_peak`（Controller 最低门槛 `affair:announce_peak`） | Controller `affair:announce_peak` + Service 细粒度校验 | 匹配 |
| 编辑/删除公告 | `affair:manage_own_peak` + 发布者/全局角色 | `@RequiresPermission("affair:manage_own_peak")` + Service 校验 | 匹配 |
| 创建活动 | `affair:create_event` | `@RequiresPermission("affair:create_event")` + Service `hasPermission("affair:create_event")` | 匹配 |
| 编辑/删除活动 | `affair:manage_own_peak` + 组织者/全局/本峰 | `@RequiresPermission("affair:manage_own_peak")` + Service 四条件校验 | 匹配 |
| 获取所有峰 | 登录即可 | `getAllPeaks` 无 `@RequiresPermission`（仅需登录） | 匹配 |
| 报名活动 | `affair:view` | `@RequiresPermission("affair:view")` | 匹配（已实现） |
| 结束活动 | `affair:manage_own_peak` | — | ⚠️ 接口未实现，权限注解暂不存在 |

> 小结：已实现的公告与活动 CRUD 接口、`joinEvent` 报名接口、`GET /api/rbac/peaks` 的权限要求与文档一致；仅 `endEvent` 接口因后端未实现，其权限要求尚无法在代码层验证。
