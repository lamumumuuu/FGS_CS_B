# 计算机协会管理系统 - 数据库管理文档

## 一、概述

本文档详细描述了计算机协会管理系统的数据库设计、操作流程和管理策略。系统采用 **PostgreSQL 13+** 作为数据库管理系统，基于 **RBAC（Role-Based Access Control）** 权限模型构建，支持宗门事务管理、任务大厅、弟子名册等核心业务功能。

### 技术栈

| 技术 | 版本/说明 |
|------|----------|
| 数据库 | PostgreSQL 13+ |
| ORM框架 | MyBatis-Plus |
| 缓存 | Redis 6+（登录锁定） |
| 字符编码 | UTF-8 |

### 数据库初始化顺序

```
1. Users.sql           → 创建用户表
2. rbac_init.sql       → 创建RBAC相关表（峰、权限、角色、关联表）并初始化数据
3. task_and_sect_init.sql → 创建任务表和弟子表
```

---

## 二、数据库表结构设计

### 2.1 表关系图

```
┌─────────────┐     ┌─────────────┐     ┌──────────────────┐
│   users     │     │   roles     │     │   permissions    │
├─────────────┤     ├─────────────┤     ├──────────────────┤
│ id (PK)     │     │ id (PK)     │     │ id (PK)          │
│ username    │     │ name        │     │ name             │
│ password    │     │ display_name│     │ display_name     │
│ status      │     │ level       │     │ module           │
│ ...         │     │ is_system   │     │ ...              │
└──────┬──────┘     └──────┬──────┘     └────────┬─────────┘
       │                   │                     │
       │ 1:N               │ 1:N                │ 1:N
       ▼                   ▼                     ▼
┌───────────────┐  ┌───────────────────┐  ┌───────────────────┐
│  user_roles   │  │  role_permissions │  │                   │
├───────────────┤  ├───────────────────┤  │                   │
│ id (PK)       │  │ id (PK)           │  │                   │
│ user_id (FK)  │  │ role_id (FK)      │  │                   │
│ role_id (FK)  │  │ permission_id (FK)│  │                   │
│ peak_id (FK)  │  └───────────────────┘  └───────────────────┘
└───────────────┘
       │
       │
       ▼
┌───────────────┐     ┌─────────────┐
│  disciples    │     │   peaks     │
├───────────────┤     ├─────────────┤
│ id (PK)       │     │ id (PK)     │
│ user_id (FK)  │     │ name        │
│ name          │     │ description │
│ role          │     │ ...         │
│ peak          │     └─────────────┘
│ lingshi       │
│ ...           │
└───────────────┘
       │
       │
       ▼
┌───────────────┐
│    tasks      │
├───────────────┤
│ id (PK)       │
│ title         │
│ publisher_id  │
│ completer_id  │
│ peak_id (FK)  │
│ status        │
│ reward        │
│ ...           │
└───────────────┘
       │
       │
       ▼
┌───────────────┐
│  audit_logs   │
├───────────────┤
│ id (PK)       │
│ operator_id   │
│ operation     │
│ module        │
│ ...           │
└───────────────┘
```

### 2.2 表结构详细说明

#### 1. 用户表（users）

**用途**：存储系统用户的基本登录信息。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名，唯一标识 |
| password | VARCHAR(100) | NOT NULL | BCrypt加密后的密码 |
| avatar | VARCHAR(255) | NULL | 头像URL或路径 |
| role | INTEGER | DEFAULT 0 | 角色编号（旧字段，已弃用） |
| status | INTEGER | DEFAULT 1 | 用户状态：1=正常，0=禁用 |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| last_login_time | TIMESTAMP | NULL | 最后登录时间 |

**索引**：
- `idx_users_username`：加速用户名查询（登录时使用）
- `idx_users_status`：加速状态筛选

---

#### 2. 峰表（peaks）

**用途**：存储宗门各峰的信息，支持动态增删。兼具宗门总可支配灵石的持久化容器（通过 `_TREASURY_` 特殊峰实现）。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| name | VARCHAR(32) | NOT NULL, UNIQUE | 峰名称，唯一 |
| description | VARCHAR(255) | NULL | 峰描述信息 |
| total_lingshi | BIGINT | DEFAULT 0 | 峰累计灵石（历史累计值） |
| available_lingshi | BIGINT | DEFAULT 0 | 峰可支配灵石（当前可用余额） |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**峰分类**：
- **业务峰**：项目峰、算法峰、电路峰 —— 作为业务实体参与灵石分配、调拨与统计
- **系统控制面板（受保护）**：管理台 —— 非业务峰，不出现在"所属峰"选择器与财务调拨/分配目标中，受系统保护，不可删除
- **财务特殊峰（受保护）**：`_TREASURY_` —— 作为"宗门总可支配灵石"的持久化容器，不出现在任何业务列表中，受系统保护，不可删除

**初始化数据**：
| id | name | description | available_lingshi | 受保护 |
|----|------|-------------|-------------------|--------|
| 1 | 管理台 | 协会管理决策中枢 | 0 | ✓ |
| 2 | 项目峰 | 项目开发与技术攻关 | 0 | |
| 3 | 算法峰 | 算法研究与竞赛 | 0 | |
| 4 | 电路峰 | 硬件设计与嵌入式开发 | 0 | |
| 5 | _TREASURY_ | 宗门总可支配灵石特殊峰 | 100000 | ✓ |

**财务特殊峰 `_TREASURY_` 详解**：

`_TREASURY_` 是 peaks 表中的一个特殊峰记录，用于持久化宗门公共可自由支配灵石总量，是宗门财务中枢的资金容器。

| 维度 | 说明 |
|------|------|
| 存储字段 | `available_lingshi` 字段存储总可支配灵石数量 |
| 业务可见性 | 不出现在"所属峰"选择器、财务调拨/分配目标等任何业务列表中 |
| 内存缓存 | 通过 `FinanceController` 中的 `AtomicLong` 计数器在内存中缓存当前总量，避免频繁读库 |
| 数据库同步 | 通过 `incrementTotalDisposable` / `decrementTotalDisposable` 方法将内存变更同步回数据库 |
| 流水记录 | 调整总可支配灵石时（`POST /api/finance/adjust-total-disposable`），在此峰上记录 `adjust_in` / `adjust_out` 类型的灵石流水（`disciple_id` 为 NULL，`peak_id` 指向 `_TREASURY_`） |

**典型操作流程**：

```
调整总可支配灵石（POST /api/finance/adjust-total-disposable）
   ↓
FinanceController 校验权限与参数
   ↓
更新内存 AtomicLong 计数器（increment / decrement）
   ↓
同步更新 peaks.available_lingshi（_TREASURY_ 记录）
   ↓
写入 lingshi_transactions 流水
   ├─ type: adjust_in（增加） / adjust_out（减少）
   ├─ disciple_id: NULL（系统级操作）
   ├─ peak_id: _TREASURY_ 峰ID
   └─ amount: 变更金额
```

**峰级灵石数据独立性**：

系统中存在两个独立的灵石存储维度，二者分别追踪、互不干扰：

| 维度 | 存储位置 | 说明 |
|------|----------|------|
| 总可支配灵石 | `_TREASURY_` 峰的 `available_lingshi` 字段 | 宗门全局可自由支配的灵石总量，由财务统一管理 |
| 峰可支配灵石 | 各业务峰的 `available_lingshi` 字段 | 各峰独立持有的可支配灵石，由峰自主管理 |

> **重要**：二者是独立记录的数值，不存在自动汇总关系。`_TREASURY_` 的灵石不会自动等于各峰灵石之和，需要通过分配/退回操作进行流转。

**峰灵石字段语义对照**：

不同类型峰的 `available_lingshi` 与 `total_lingshi` 字段语义存在差异：

| 峰类型 | `available_lingshi` | `total_lingshi` |
|--------|---------------------|------------------|
| `_TREASURY_` 特殊峰 | 宗门总可支配灵石（资金容器） | 不参与业务语义 |
| 业务峰 | 峰可支配灵石（当前可用余额） | 峰累计灵石（历史累计值） |

**关键流转规则**：

| 流水类型 | 流转方向 | 字段变更 |
|----------|----------|----------|
| `allocate_in` | `_TREASURY_` → 业务峰 | `_TREASURY_.available_lingshi` 减少，业务峰 `available_lingshi` 增加 |
| `allocate_to_member` | 业务峰 → 弟子 | 业务峰 `available_lingshi` 减少，弟子 `lingshi` 增加 |
| `peak_return_out`/`peak_return_in` | 业务峰 → `_TREASURY_` | 业务峰 `available_lingshi` 减少，`_TREASURY_.available_lingshi` 增加 |
| `peak_transfer_out`/`peak_transfer_in` | 业务峰 → 业务峰 | 源峰 `available_lingshi` 减少，目标峰 `available_lingshi` 增加 |

**峰灵石退回（return-from-peak）数据流**：

当业务峰将灵石退回至宗门总可支配灵石池时，执行以下操作流程：

```
峰灵石退回请求（POST /api/finance/return-to-treasury）
   ↓
FinanceController 校验权限、目标峰合法性与金额充足性
   ↓
【1】扣减业务峰灵石
   ├─ 目标业务峰.available_lingshi -= amount
   └─ 记录 LingshiTransaction（peak_return_out 类型）
        ├─ peak_id: 业务峰ID
        ├─ peak_name: 业务峰名称
        ├─ amount: -amount（负数，表示支出）
        └─ balance: 变更后业务峰余额
   ↓
【2】增加 _TREASURY_ 灵石
   ├─ _TREASURY_.available_lingshi += amount
   └─ 记录 LingshiTransaction（peak_return_in 类型）
        ├─ peak_id: _TREASURY_ 峰ID
        ├─ peak_name: _TREASURY_
        ├─ amount: +amount（正数，表示收入）
        └─ balance: 变更后 _TREASURY_ 余额
```

**退回操作数据保护规则**：

| 规则 | 说明 |
|------|------|
| 金额上限 | 退回金额不得超过目标业务峰的 `available_lingshi`，否则请求将被拒绝 |
| 保护峰排除 | `管理台` 和 `_TREASURY_` 作为系统受保护峰，不可作为退回操作的源峰 |
| 原子性保证 | 扣减业务峰与增加 `_TREASURY_` 在同一事务中执行，确保数据一致性 |
| 权限校验 | 仅具备 `finance:adjust_lingshi` 权限的角色可执行退回操作 |

**峰灵石分配至弟子（allocate-to-member）数据流**：

当业务峰将灵石分配给本峰弟子时，执行以下操作流程：

```
峰灵石分配至弟子请求（POST /api/finance/allocate-to-member）
   ↓
FinanceController 校验权限、源峰合法性、弟子归属与金额充足性
   ↓
【1】扣减源业务峰灵石
   ├─ 源业务峰.available_lingshi -= amount
   └─ 记录 LingshiTransaction（allocate_to_member 类型）
        ├─ disciple_id: 接收弟子ID
        ├─ disciple_name: 接收弟子姓名
        ├─ type: allocate_to_member
        ├─ amount: -amount（负数，表示峰支出）
        ├─ balance: 变更后弟子灵石余额
        ├─ operator_id: 操作人ID
        ├─ operator_name: 操作人姓名
        ├─ remark: 备注信息
        ├─ peak_id: 源业务峰ID
        └─ peak_name: 源业务峰名称
   ↓
【2】增加弟子灵石
   └─ 弟子.lingshi += amount
```

**分配至弟子操作数据保护规则**：

| 规则 | 说明 |
|------|------|
| 金额上限 | 分配金额不得超过源业务峰的 `available_lingshi`，否则请求将被拒绝 |
| 弟子归属校验 | 接收弟子必须归属于源业务峰，跨峰分配将被拒绝 |
| 保护峰排除 | `管理台` 和 `_TREASURY_` 作为系统受保护峰，不可作为分配的源峰 |
| 原子性保证 | 扣减业务峰 `available_lingshi` 与增加弟子 `lingshi` 在同一事务中执行，确保数据一致性 |
| 权限校验 | 仅具备 `finance:adjust_lingshi` 权限的角色可执行分配操作 |

> **说明**：`allocate_to_member` 与 `allocate_in` 不同——`allocate_in` 是 `_TREASURY_` 向业务峰下发灵石（峰→峰），而 `allocate_to_member` 是业务峰向本峰弟子下发灵石（峰→弟子）。前者增加业务峰 `available_lingshi`，后者减少业务峰 `available_lingshi` 并增加弟子 `lingshi`。

---

#### 3. 权限表（permissions）

**用途**：存储细粒度权限定义，采用 `资源:操作` 命名规范。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| name | VARCHAR(64) | NOT NULL, UNIQUE | 权限标识（如 `quest:create_draft`） |
| display_name | VARCHAR(64) | NOT NULL | 中文显示名称 |
| module | VARCHAR(32) | NOT NULL | 所属模块 |
| description | VARCHAR(255) | NULL | 权限描述 |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**权限模块分类**：

| 模块 | 权限数量 | 说明 |
|------|----------|------|
| quest | 13 | 任务相关权限 |
| member | 6 | 成员管理权限 |
| affair | 4 | 宗门事务权限 |
| peak | 4 | 峰管理权限 |
| finance | 5 | 财务权限 |
| system | 1 | 系统管理权限 |

**完整权限列表**：

```
任务模块（quest）：
├── quest:view_all           → 查看所有任务
├── quest:view_own_peak      → 查看本峰任务
├── quest:create_global      → 发布全协会悬赏
├── quest:create_peak        → 发布本峰悬赏
├── quest:create_draft       → 发布任务草稿
├── quest:edit_any           → 编辑任意任务
├── quest:edit_own_peak      → 编辑本峰任务
├── quest:delete_any         → 删除任意任务
├── quest:delete_own_peak    → 删除本峰任务
├── quest:accept             → 接取任务
├── quest:submit             → 提交任务成果
├── quest:review             → 审核/验收任务
└── quest:force_close        → 强制结项

成员模块（member）：
├── member:view_all          → 查看全协会成员
├── member:view_own_peak     → 查看本峰成员
├── member:approve_join      → 审批入会申请
├── member:update_role       → 修改成员角色
├── member:appoint_elder     → 任命/撤换长老
└── member:remove            → 开除成员

宗门事务模块（affair）：
├── affair:announce_global   → 发布宗门公告
├── affair:announce_peak     → 发布本峰公告
├── affair:event_create      → 创建活动
└── affair:event_manage      → 管理所有活动

峰管理模块（peak）：
├── peak:create              → 创建/解散峰
├── peak:edit_any            → 修改任意峰信息
├── peak:edit_own            → 修改本峰信息
└── peak:add_member          → 峰成员管理

财务模块（finance）：
├── finance:view_all         → 查看全协会财务
├── finance:view_own_peak    → 查看本峰财务
├── finance:adjust_lingshi  → 调整灵石
└── finance:set_base         → 设定悬赏基准

系统模块（system）：
└── system:admin             → 系统管理
```

---

#### 4. 角色表（roles）

**用途**：存储系统角色定义，支持7种固定角色。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| name | VARCHAR(32) | NOT NULL, UNIQUE | 角色标识（如 `sect_master`） |
| display_name | VARCHAR(32) | NOT NULL | 中文显示名称 |
| description | VARCHAR(255) | NULL | 角色描述 |
| level | SMALLINT | NOT NULL | 角色层级：0=最高，数值越大权限越低 |
| is_system | SMALLINT | DEFAULT 1 | 是否系统内置：1=是（不可删除），0=否 |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**角色定义**：

| id | name | display_name | description | level | is_system |
|----|------|--------------|-------------|-------|-----------|
| 1 | sect_master | 宗主 | 系统唯一超级管理员，当任会长 | 0 | 1 |
| 2 | grand_elder | 大长老 | 协助宗主打理全局，副会长 | 1 | 1 |
| 3 | supreme_elder | 太上长老 | 历届会长/重大贡献管理层 | 1 | 1 |
| 4 | honor_elder | 荣誉长老 | 其他历届管理层，荣誉称号 | 1 | 1 |
| 5 | elder | 长老 | 各组组长/其他管理层，有峰级限定 | 1 | 1 |
| 6 | inner_disciple | 内门弟子 | 各组核心骨干，有峰级限定 | 2 | 1 |
| 7 | outer_disciple | 外门弟子 | 普通会员，基础权限 | 2 | 1 |

---

#### 5. 角色-权限关联表（role_permissions）

**用途**：实现角色与权限的多对多关系。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| role_id | INT | NOT NULL, FK→roles(id) | 角色ID |
| permission_id | INT | NOT NULL, FK→permissions(id) | 权限ID |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**约束**：
- `UNIQUE (role_id, permission_id)`：防止重复授权
- `ON DELETE CASCADE`：角色或权限删除时自动级联删除关联

---

#### 6. 用户-角色关联表（user_roles）

**用途**：实现用户与角色的多对多关系，支持用户拥有多个角色。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| user_id | BIGINT | NOT NULL | 用户ID（关联users表） |
| role_id | INT | NOT NULL, FK→roles(id) | 角色ID |
| peak_id | INT | NULL, FK→peaks(id) | 所属峰ID（NULL表示全局角色） |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**约束**：
- `ON DELETE SET NULL`：峰被删除时peak_id字段置空

**说明**：
- `peak_id` 为 NULL 表示该角色为全局角色，不受峰限制
- 一个用户可拥有多个角色（如某人既是荣誉长老，又兼任某峰长老）
- 长老（elder）和内门弟子（inner_disciple）角色必须指定peak_id
- **全局角色判定**：除 `peak_id IS NULL` 外，系统还会通过角色名称判定全局角色用户。拥有 `sect_master`、`grand_elder`、`supreme_elder`、`honor_elder` 角色名的用户即使存在峰绑定记录，也会被 `PermissionServiceImpl.isGlobalRoleUser()` 识别为全局角色用户（详见 5.2.1 节）

---

#### 7. 弟子表（disciples）

**用途**：存储宗门弟子信息，与用户表一对一关联。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| user_id | BIGINT | NULL | 关联users表的用户ID |
| name | VARCHAR(64) | NOT NULL | 弟子姓名 |
| student_id | VARCHAR(32) | DEFAULT '' | 学号 |
| role | VARCHAR(32) | NOT NULL, DEFAULT 'outer_disciple' | 宗门角色标识 |
| peak | VARCHAR(32) | DEFAULT '无' | 所属峰名称 |
| lingshi | BIGINT | DEFAULT 0 | 灵石数量（余额） |
| joined_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 加入时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- `idx_disciples_user_id`：加速按用户查询
- `idx_disciples_name`：加速姓名搜索
- `idx_disciples_peak`：加速按峰筛选
- `idx_disciples_role`：加速按角色筛选

**添加弟子自动用户创建流程**：

通过系统接口添加新弟子时，会自动执行以下流程，确保用户账号与弟子记录的一致性：

```
添加弟子请求（POST /api/member/add-disciple）
   ↓
检查用户名是否已存在（防止重复创建）
   ↓
在 users 表中创建用户记录
   ├─ username：使用弟子姓名（或指定用户名）
   ├─ password：默认密码 "123456"（BCrypt 加密存储）
   ├─ status：1（正常）
   └─ 其他字段：使用默认值
   ↓
获取新创建的 user.id
   ↓
在 disciples 表中创建弟子记录
   ├─ user_id：关联新创建的用户ID
   ├─ name：弟子姓名
   ├─ peak：指定所属峰
   ├─ role：默认 outer_disciple（外门弟子）
   └─ 其他字段：使用默认值
   ↓
在 user_roles 表中创建角色关联
   ├─ user_id：新用户ID
   ├─ role_id：外门弟子角色ID
   └─ peak_id：指定峰ID
   ↓
返回成功（含用户ID和弟子ID）
```

> **重要**：新创建的用户默认密码为 `123456`，首次登录后应立即修改密码。管理员在添加弟子时可指定初始密码，未指定时使用默认值。

---

#### 8. 任务表（tasks）

**用途**：存储悬赏任务信息，支持完整的任务生命周期管理。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| title | VARCHAR(100) | NOT NULL | 任务标题 |
| description | TEXT | NOT NULL | 任务描述 |
| difficulty | VARCHAR(16) | NOT NULL, DEFAULT '黑铁' | 难度等级 |
| status | VARCHAR(16) | NOT NULL, DEFAULT '审核中' | 任务状态 |
| reward | INTEGER | NOT NULL, DEFAULT 0 | 悬赏灵石数量 |
| deadline | TIMESTAMP | NULL | 截止日期 |
| publisher_id | BIGINT | NULL | 发布者ID（关联users表） |
| completer_id | BIGINT | NULL | 完成者ID（关联users表） |
| tech_requirements | TEXT | NULL | 技术要求（JSON或逗号分隔） |
| peak_id | INTEGER | NULL, FK→peaks(id) | 所属峰ID |
| reject_reason | TEXT | NULL | 驳回原因 |
| reviewed_by | BIGINT | NULL | 审核人ID |
| reviewed_at | TIMESTAMP | NULL | 审核时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**难度等级**：黑铁 → 青铜 → 白银 → 黄金

**任务状态机**：
```
审核中 → 等待中 → 讨伐中 → 已完成
            ↓            ↓
          已驳回      已驳回
```

**索引**：
- `idx_tasks_status`：加速状态筛选
- `idx_tasks_difficulty`：加速难度筛选
- `idx_tasks_publisher_id`：加速按发布者查询
- `idx_tasks_completer_id`：加速按完成者查询
- `idx_tasks_peak_id`：加速按峰筛选
- `idx_tasks_created_at`：加速时间排序
- `idx_tasks_title`：加速标题搜索

---

#### 9. 灵石流水表（lingshi_transactions）

**用途**：记录所有灵石增减变动明细，支撑财务审计与"灵石调整日志"视图。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| disciple_id | BIGINT | NULL | 弟子ID（系统级操作可为空） |
| disciple_name | VARCHAR(64) | NULL | 弟子姓名（冗余存储） |
| type | VARCHAR(32) | NOT NULL | 变更类型，支持 `reward`/`task_reward`/`adjust_in`/`adjust_out`/`allocate_in`/`allocate_to_member`/`peak_transfer_in`/`peak_transfer_out`/`peak_return_in`/`peak_return_out` 等（详见下方枚举） |
| amount | BIGINT | NOT NULL | 变更金额（正数增加，负数减少） |
| balance | BIGINT | NULL | 变更后余额 |
| operator_id | BIGINT | NULL | 操作人ID |
| operator_name | VARCHAR(64) | NULL | 操作人姓名 |
| remark | TEXT | NULL | 备注 |
| peak_id | BIGINT | NULL, FK→peaks(id) | 关联峰ID（峰级分类统计用） |
| peak_name | VARCHAR(32) | NULL | 关联峰名称（冗余存储） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**变更类型枚举**：

| 类型 | 说明 | 使用场景 |
|------|------|----------|
| `reward` | 打赏奖励（已移除） | 原"打赏弟子"功能，该功能已下线；历史数据中可能仍存在此类型记录，但不再产生新记录 |
| `task_reward` | 任务完成赏金 | 任务审核通过后发放悬赏（仍在使用，与已移除的 `reward` 类型不同，请勿混淆） |
| `adjust_in` | 灵石增加 | 弟子灵石调整增加、总可支配灵石调整增加 |
| `adjust_out` | 灵石扣除 | 弟子灵石调整减少、总可支配灵石调整减少 |
| `allocate_in` | 总可支配灵石分配至峰 | 总可支配灵石向业务峰下发 |
| `allocate_to_member` | 峰灵石分配至弟子 | 峰向本峰弟子下发灵石（峰→弟子） |
| `peak_transfer_in` | 峰间调拨收入 | 业务峰之间调拨的收入方 |
| `peak_transfer_out` | 峰间调拨支出 | 业务峰之间调拨的支出方 |
| `peak_return_in` | 峰灵石退回收入 | 业务峰将灵石退回至 `_TREASURY_` 时，`_TREASURY_` 侧的收入记录 |
| `peak_return_out` | 峰灵石退回支出 | 业务峰将灵石退回至 `_TREASURY_` 时，业务峰侧的支出记录 |

**`adjust_in` / `adjust_out` 双场景说明**：

该类型同时承担两类调整场景，通过 `disciple_id` 与 `peak_id` 区分：

1. **弟子灵石调整**：直接调整弟子个人灵石余额
   - `disciple_id` 非 NULL，记录被调整的弟子
   - `peak_id` 关联弟子所属峰，用于峰级财务统计
2. **总可支配灵石调整**：调整宗门公共可自由支配灵石总量
   - `disciple_id` 为 NULL（系统级操作）
   - `peak_id` 关联 `_TREASURY_` 特殊峰（详见 2.2 节"财务特殊峰"说明）
   - 触发接口：`POST /api/finance/adjust-total-disposable`

**索引**：
- `idx_lt_type`：按变更类型筛选
- `idx_lt_peak_id`：按峰筛选（"各峰财务"视图）
- `idx_lt_operator_id`：按操作人筛选
- `idx_lt_created_at`：按时间排序

---

#### 10. 公告表（announcements）

**用途**：存储宗门公告（含宗门公告与本峰公告）。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| title | VARCHAR(100) | NOT NULL | 公告标题 |
| content | TEXT | NOT NULL | 公告内容 |
| publisher_id | BIGINT | NULL | 发布者ID |
| publisher_name | VARCHAR(64) | NULL | 发布者名称（冗余存储） |
| peak_id | BIGINT | NULL, FK→peaks(id) | 关联峰ID（全局公告为NULL） |
| type | VARCHAR(16) | NOT NULL, DEFAULT 'global' | 公告类型：global-全协会, peak-本峰 |
| status | VARCHAR(16) | NOT NULL, DEFAULT 'published' | 状态：published-已发布, draft-草稿 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- `idx_ann_type`：按类型筛选
- `idx_ann_peak_id`：按峰筛选本峰公告
- `idx_ann_status`：按状态筛选

---

#### 11. 活动表（events）

**用途**：存储宗门活动（全协会活动与本峰活动）。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| name | VARCHAR(100) | NOT NULL | 活动名称 |
| description | TEXT | NULL | 活动描述 |
| organizer_id | BIGINT | NULL | 组织者ID |
| organizer_name | VARCHAR(64) | NULL | 组织者名称（非持久化字段，Java @TableField(exist=false)） |
| peak_id | BIGINT | NULL, FK→peaks(id) | 关联峰ID（全局活动为NULL） |
| type | VARCHAR(16) | NOT NULL, DEFAULT 'global' | 活动类型：global-宗门活动, peak-本峰活动 |
| status | VARCHAR(16) | NOT NULL, DEFAULT 'planned' | 状态：planned-已规划, ongoing-进行中, completed-已结束, cancelled-已取消 |
| start_time | TIMESTAMP | NULL | 开始时间 |
| end_time | TIMESTAMP | NULL | 结束时间 |
| location | VARCHAR(255) | NULL | 活动地点 |
| max_participants | INTEGER | NULL, DEFAULT 50 | 最大参与人数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- `idx_events_peak_id`：按峰筛选
- `idx_events_status`：按状态筛选
- `idx_events_created_at`：按时间排序

---

#### 12. 审计日志表（audit_logs）

**用途**：记录所有敏感操作，支持安全审计和追溯。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | SERIAL | PRIMARY KEY | 自增主键 |
| operator_id | BIGINT | NULL | 操作人ID |
| operator_name | VARCHAR(64) | NULL | 操作人名称 |
| operation | VARCHAR(128) | NOT NULL | 操作描述 |
| module | VARCHAR(32) | NULL | 操作模块 |
| target_type | VARCHAR(64) | NULL | 操作目标类型 |
| target_id | BIGINT | NULL | 操作目标ID |
| before_data | TEXT | NULL | 操作前数据（JSON格式） |
| after_data | TEXT | NULL | 操作后数据（JSON格式） |
| ip_address | VARCHAR(64) | NULL | 操作IP地址 |
| user_agent | VARCHAR(512) | NULL | 客户端浏览器信息 |
| status | SMALLINT | DEFAULT 1 | 操作结果：1=成功，0=失败 |
| error_message | TEXT | NULL | 失败时的错误信息 |
| create_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

**索引**：
- `idx_audit_logs_operator_id`：加速按操作人查询
- `idx_audit_logs_module`：加速按模块筛选
- `idx_audit_logs_create_time`：加速时间范围查询

---

## 三、角色权限矩阵

### 权限分配总览

| 权限标识 | 宗主 | 大长老 | 太上长老 | 荣誉长老 | 长老(峰主) | 内门弟子 | 外门弟子 |
|----------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **任务模块** | | | | | | | |
| quest:view_all | ✓ | ✓ | ✓ | ✓ | - | - | - |
| quest:view_own_peak | ✓ | ✓ | ✓ | - | ✓ | ✓ | ✓ |
| quest:create_global | ✓ | ✓ | - | - | - | - | - |
| quest:create_peak | ✓ | ✓ | - | - | ✓ | - | - |
| quest:create_draft | - | ✓ | - | - | - | ✓ | - |
| quest:edit_any | ✓ | ✓ | - | - | - | - | - |
| quest:edit_own_peak | ✓ | ✓ | - | - | ✓ | - | - |
| quest:delete_any | ✓ | ✓ | - | - | - | - | - |
| quest:delete_own_peak | ✓ | ✓ | - | - | ✓ | - | - |
| quest:accept | ✓ | ✓ | - | - | ✓ | ✓ | ✓ |
| quest:submit | ✓ | ✓ | - | - | ✓ | ✓ | ✓ |
| quest:review | ✓ | ✓ | ✓ | - | ✓ | - | - |
| quest:force_close | ✓ | ✓ | - | - | ✓ | - | - |
| **成员模块** | | | | | | | |
| member:view_all | ✓ | ✓ | ✓ | ✓ | - | - | - |
| member:view_own_peak | ✓ | ✓ | ✓ | - | ✓ | ✓ | - |
| member:approve_join | ✓ | ✓ | ✓ | - | - | - | - |
| member:update_role | ✓ | ✓ | - | - | ✓ | - | - |
| member:appoint_elder | ✓ | ✓ | - | - | - | - | - |
| member:remove | ✓ | ✓ | - | - | - | - | - |
| **宗门事务模块** | | | | | | | |
| affair:announce_global | ✓ | ✓ | - | - | - | - | - |
| affair:announce_peak | ✓ | ✓ | - | - | ✓ | - | - |
| affair:event_create | ✓ | ✓ | - | - | ✓ | - | - |
| affair:event_manage | ✓ | ✓ | - | - | - | - | - |
| **峰管理模块** | | | | | | | |
| peak:create | ✓ | ✓ | - | - | - | - | - |
| peak:edit_any | ✓ | ✓ | - | - | - | - | - |
| peak:edit_own | ✓ | ✓ | - | - | ✓ | - | - |
| peak:add_member | ✓ | ✓ | - | - | ✓ | - | - |
| **财务模块** | | | | | | | |
| finance:view_all | ✓ | ✓ | ✓ | ✓ | - | - | - |
| finance:view_own_peak | ✓ | ✓ | ✓ | - | ✓ | - | - |
| finance:adjust_lingshi | ✓ | ✓ | - | - | - | - | - |
| finance:set_base | ✓ | ✓ | - | - | - | - | - |
| **系统模块** | | | | | | | |
| system:admin | ✓ | - | - | - | - | - | - |

### 特殊说明

1. **太上长老**：定位为"德高望重的监督者"，拥有全局查看权和任务审核监督权
2. **荣誉长老**：纯查阅角色，仅能查看公开和内部信息，无任何操作权
3. **内门弟子**：可发布草稿，协助管理本峰成员（仅限查看本峰成员简单信息）
4. **峰主**：所有操作均限制在关联峰的数据范围内，通过 `peak_id` 进行数据范围过滤

### 公告发布权限说明

宗门事务模块中的公告发布权限（`affair:announce_global` 与 `affair:announce_peak`）按角色严格区分，二者为相互独立的权限：

| 角色 | `affair:announce_global`（宗门公告） | `affair:announce_peak`（本峰公告） | 说明 |
|------|:---:|:---:|------|
| 宗主（sect_master） | ✓ | ✓ | 拥有全部权限 |
| 大长老（grand_elder） | ✓ | ✓ | 拥有除 `system:admin` 外的全部权限 |
| 长老（elder） | ✗ | ✓ | 仅可发布本峰公告，不可发布宗门公告 |
| 太上长老（supreme_elder） | ✗ | ✗ | 仅查看（view），无任何公告发布权限 |
| 荣誉长老（honor_elder） | ✗ | ✗ | 仅查看（view），无任何公告发布权限 |

> **要点**：`affair:announce_global` 与 `affair:announce_peak` 是两份独立权限，拥有本峰公告权限并不意味着拥有宗门公告权限。大长老同时具备二者；长老仅具备本峰公告权限；太上长老与荣誉长老作为纯查阅角色，均不具备任何公告发布权限。

---

## 四、数据库操作方法

### 4.1 数据库自动初始化机制（推荐）

系统在启动时会自动检测并初始化数据库，无需手动执行 SQL 脚本。

#### 自动初始化流程

```
应用启动
   ↓
DatabaseAutoInitializer (执行顺序: 0)
   ↓
检测数据库是否存在
   ├─ 存在 → 检测表结构完整性
   │            ├─ 完整 → 跳过初始化
   │            └─ 不完整 → 执行初始化脚本
   └─ 不存在 → 自动创建数据库
                  ↓
             初始化表结构和数据
                  ↓
DataInitializer (执行顺序: 1)
   ↓
创建默认管理员账号 (admin/admin123)
   ↓
应用启动完成
```

#### 自动初始化内容

**1. 数据库创建（若不存在）**

```sql
-- 自动创建数据库，指定字符集和排序规则
CREATE DATABASE sect_db 
ENCODING 'UTF8' 
LC_COLLATE = 'zh_CN.UTF-8' 
LC_CTYPE = 'zh_CN.UTF-8' 
TEMPLATE template0;

-- 若指定字符集不支持，自动降级为默认配置
CREATE DATABASE sect_db ENCODING 'UTF8';
```

**2. 表结构初始化（按顺序执行）**

| 顺序 | 脚本 | 创建内容 |
|------|------|----------|
| 1 | `sql/Users.sql` | `users` 表（用户基础信息） |
| 2 | `sql/rbac_init.sql` | RBAC 相关表（峰、权限、角色等）及初始数据 |
| 3 | `sql/task_and_sect_init.sql` | 任务表、弟子表、财务表、公告表、活动表 |

**3. 初始数据填充**

- 5 个峰（含 `_TREASURY_` 特殊峰）
- 38 条权限定义
- 7 种系统角色
- 角色-权限关联（自动分配）
- 4 条示例任务数据

#### 数据库连接配置

**application.yml 配置**：

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sect_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
```

**环境变量说明**：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `DB_HOST` | 数据库主机 | `localhost` |
| `DB_PORT` | 数据库端口 | `5432` |
| `DB_NAME` | 数据库名 | `sect_db` |
| `DB_USERNAME` | 数据库用户名 | `postgres` |
| `DB_PASSWORD` | 数据库密码（必须配置） | 无 |

#### 所需数据库权限

自动初始化功能需要数据库用户具备以下权限：

| 权限 | 用途 | 说明 |
|------|------|------|
| `CREATEDB` | 创建数据库 | 用于自动创建目标数据库 |
| `CONNECT` | 连接数据库 | 连接到默认数据库进行操作 |
| `CREATE` | 创建表结构 | 在目标数据库中创建表 |
| `INSERT` | 插入数据 | 填充初始数据 |
| `INDEX` | 创建索引 | 优化查询性能 |

**权限授予示例**：

```sql
-- 授予 CREATEDB 权限（超级管理员执行）
ALTER USER dbuser CREATEDB;

-- 或授予完整权限（不推荐生产环境）
GRANT ALL PRIVILEGES ON DATABASE postgres TO dbuser;
```

#### 错误处理机制

系统会捕获并处理以下异常情况：

**1. 连接失败**

```
错误类型：数据库连接失败
可能原因：
  - 数据库服务未启动
  - 网络不通
  - 认证失败
处理方式：记录详细日志，终止启动
提示信息：请检查数据库服务状态和连接配置
```

**2. 权限不足**

```
错误类型：SecurityException
可能原因：
  - 数据库用户无 CREATEDB 权限
  - 无创建表权限
处理方式：记录详细日志，终止启动
提示信息：请检查数据库用户权限：需要 CREATEDB 权限
```

**3. SQL 执行错误**

```
错误类型：SQLException
错误信息：
  - SQL 状态码
  - 错误代码
  - 详细错误信息
处理方式：记录完整 SQL 错误信息，终止启动
提示信息：SQL 执行错误，请检查脚本内容
```

**4. 资源耗尽**

```
错误类型：OutOfMemoryError
可能原因：
  - 系统内存不足
  - 数据库连接池耗尽
处理方式：记录错误，终止启动
提示信息：系统资源不足，请增加服务器资源
```

#### 日志输出示例

**成功初始化日志**：

```
========================================
开始执行数据库存在性检测与初始化...
========================================
检测数据库 [sect_db] 是否存在...
数据库 [sect_db] 不存在，开始自动创建...
开始创建数据库 [sect_db]...
数据库 [sect_db] 创建成功
开始执行数据库初始化脚本...
执行脚本: sql/Users.sql
脚本 [sql/Users.sql] 执行成功
执行脚本: sql/rbac_init.sql
脚本 [sql/rbac_init.sql] 执行成功
执行脚本: sql/task_and_sect_init.sql
脚本 [sql/task_and_sect_init.sql] 执行成功
所有初始化脚本执行完成
========================================
  数据库初始化成功！
  数据库名: sect_db
  字符集: UTF-8
  排序规则: 默认
  表结构: 已创建
  初始数据: 已填充
========================================
```

**数据库已存在日志**：

```
========================================
开始执行数据库存在性检测与初始化...
========================================
检测数据库 [sect_db] 是否存在...
数据库 [sect_db] 已存在，跳过创建
检测核心表结构...
所有核心表存在
数据库表结构完整，跳过初始化
========================================
```

---

### 4.2 手动初始化数据库（备用方案）

如果自动初始化失败或需要手动控制，可使用以下方法。

**步骤1：创建数据库**

```sql
CREATE DATABASE sect_db;
\c sect_db;
```

**步骤2：执行初始化脚本（按顺序）**

```bash
# 方式1：使用psql命令行
psql -U username -d sect_db -f src/main/resources/sql/Users.sql
psql -U username -d sect_db -f src/main/resources/sql/rbac_init.sql
psql -U username -d sect_db -f src/main/resources/sql/task_and_sect_init.sql

# 方式2：在psql交互模式中
\i src/main/resources/sql/Users.sql
\i src/main/resources/sql/rbac_init.sql
\i src/main/resources/sql/task_and_sect_init.sql
```

### 4.3 常用查询操作

**查询当前用户角色和权限**：

```sql
-- 获取用户拥有的角色
SELECT r.name, r.display_name, r.level, p.name as peak_name
FROM user_roles ur
JOIN roles r ON ur.role_id = r.id
LEFT JOIN peaks p ON ur.peak_id = p.id
WHERE ur.user_id = 1;

-- 获取用户拥有的权限
SELECT DISTINCT p.name, p.display_name, p.module
FROM user_roles ur
JOIN role_permissions rp ON ur.role_id = rp.role_id
JOIN permissions p ON rp.permission_id = p.id
WHERE ur.user_id = 1;
```

**查询任务列表**：

```sql
-- 查询所有等待中的任务
SELECT t.*, u.username as publisher_name, p.name as peak_name
FROM tasks t
LEFT JOIN users u ON t.publisher_id = u.id
LEFT JOIN peaks p ON t.peak_id = p.id
WHERE t.status = '等待中'
ORDER BY t.created_at DESC;

-- 查询我发布的任务
SELECT * FROM tasks WHERE publisher_id = 1 ORDER BY created_at DESC;

-- 查询我接取的任务
SELECT * FROM tasks WHERE completer_id = 1 ORDER BY updated_at DESC;
```

**查询弟子列表**：

```sql
-- 查询所有弟子
SELECT d.* FROM disciples d ORDER BY d.joined_at DESC;

-- 查询某峰弟子
SELECT d.* FROM disciples d WHERE d.peak = '项目峰';

-- 查询管理层弟子
SELECT d.* FROM disciples d WHERE d.role IN ('sect_master', 'grand_elder', 'supreme_elder', 'honor_elder', 'elder');
```

**查询审计日志**：

```sql
-- 查询最近操作记录
SELECT * FROM audit_logs ORDER BY create_time DESC LIMIT 50;

-- 查询某模块操作记录
SELECT * FROM audit_logs WHERE module = 'task' ORDER BY create_time DESC;

-- 查询某用户操作记录
SELECT * FROM audit_logs WHERE operator_id = 1 ORDER BY create_time DESC;
```

### 4.4 数据修改操作

**添加新峰**：

```sql
INSERT INTO peaks (name, description) VALUES ('新峰名称', '峰描述') ON CONFLICT (name) DO NOTHING;
```

**删除峰**：

```sql
-- 删除峰（会触发级联操作，详见下方说明）
DELETE FROM peaks WHERE name = '要删除的峰名';
```

**峰删除级联规则**：

删除一个峰时，系统会自动执行以下级联操作，确保数据一致性：

| 序号 | 级联操作 | 说明 |
|------|----------|------|
| 1 | 移除用户-峰关联 | `user_roles` 表中关联该峰的记录的 `peak_id` 被置空（`ON DELETE SET NULL`），用户保留但失去峰级角色归属 |
| 2 | 撤销峰管理员角色 | 拥有该峰长老（elder）或内门弟子（inner_disciple）角色的用户，其峰级角色关联被撤销，**降级为普通成员**（外门弟子/普通成员角色） |
| 3 | 清除弟子峰引用 | `disciples` 表中 `peak` 字段引用该峰的记录，其峰引用被清空（设为"无"） |
| 4 | 保留业务峰数据 | 该峰关联的任务（tasks）、灵石流水（lingshi_transactions）等历史数据保留，但峰引用标记为已删除状态 |

> **注意**：受保护的峰（管理台、`_TREASURY_`）不允许删除，系统会拦截删除请求并返回错误提示。

**调整弟子灵石**：

```sql
-- 增加灵石
UPDATE disciples SET lingshi = lingshi + 100 WHERE id = 1;

-- 减少灵石（确保不小于0）
UPDATE disciples SET lingshi = GREATEST(0, lingshi - 50) WHERE id = 1;
```

**修改用户角色**：

```sql
-- 添加角色
INSERT INTO user_roles (user_id, role_id, peak_id) VALUES (1, 5, 2);

-- 移除角色
DELETE FROM user_roles WHERE user_id = 1 AND role_id = 5;
```

### 4.5 峰数据保护规则

系统对峰（peaks）表实施严格的数据保护规则，确保核心业务数据的完整性和安全性。

#### 受保护的峰

以下峰被标记为受保护，不允许通过任何接口或管理操作删除：

| 峰名称 | 保护原因 | 限制说明 |
|--------|----------|----------|
| 管理台 | 系统控制面板 | 系统核心功能依赖此峰，删除将导致管理功能异常 |
| `_TREASURY_` | 财务持久化容器 | 存储宗门总可支配灵石，删除将导致财务数据丢失 |

#### 保护规则详情

**1. 删除保护**：
- 受保护的峰**不允许删除**，任何删除请求将被系统拦截并返回错误提示
- 即使通过直接数据库操作删除，也会在应用层启动时被检测到并触发修复机制

**2. 名称保护**：
- 受保护的峰名称具有唯一性和系统性，不允许修改其 `name` 字段
- 禁止创建与受保护峰同名的新峰（`UNIQUE` 约束自动阻止）

**3. 引用完整性**：
- 业务峰的删除受 `user_roles.peak_id` 的 `ON DELETE SET NULL` 约束保护
- 删除业务峰时，关联的用户角色引用会被清空而非级联删除，保留用户数据
- 弟子表（disciples）的峰引用在峰删除时会被清空

**4. 数据恢复**：
- 误删除的业务峰可通过数据库备份恢复
- 受保护峰若被意外删除，系统在下次启动时会自动重新初始化

#### 保护机制实现

```
峰删除请求
   ↓
检查目标峰是否为受保护峰
   ├─ 是（管理台 / _TREASURY_）→ 拒绝删除，返回错误："该峰为系统保护峰，不允许删除"
   └─ 否 → 检查是否为最后一个业务峰
            ├─ 是 → 拒绝删除，返回错误："至少需保留一个业务峰"
            └─ 否 → 执行级联删除流程（见 4.4 节"峰删除级联规则"）
```

---

### 4.6 数据备份与恢复

**备份数据库**：

```bash
# 完整备份
pg_dump -U username -d sect_db -f sect_db_backup.sql

# 仅备份表结构
pg_dump -U username -d sect_db --schema-only -f sect_db_schema.sql

# 仅备份数据
pg_dump -U username -d sect_db --data-only -f sect_db_data.sql
```

**恢复数据库**：

```bash
# 方式1：创建新数据库并恢复
createdb -U username new_db
psql -U username -d new_db -f sect_db_backup.sql

# 方式2：在现有数据库中恢复（会覆盖数据）
psql -U username -d sect_db -f sect_db_backup.sql
```

**定期备份策略**：

```bash
# 每日凌晨2点自动备份（crontab）
0 2 * * * pg_dump -U dbuser -d sect_db -f /backup/sect_db_$(date +%Y%m%d).sql

# 保留最近7天备份
find /backup/sect_db_*.sql -mtime +7 -delete
```

---

## 五、权限管理策略

### 5.1 权限校验机制

系统采用 **基于注解的权限控制** + **AOP切面自动校验** 的方式：

1. **注解定义**：`@RequiresPermission(value = "quest:create_draft")`
2. **切面处理**：`PermissionAspect` 拦截所有带注解的方法
3. **校验逻辑**：
   - 从 `UserContext` 获取当前用户ID
   - 查询用户拥有的权限列表
   - 检查是否具备所需权限
   - 无权限时抛出 `BusinessException`

### 5.2 数据范围限制

对于峰级角色（长老、内门弟子），系统通过 `peak_id` 进行数据范围过滤：

1. **登录时加载**：查询 `user_roles`，提取用户关联的 `peak_id` 列表
2. **权限中间件**：
   - 检查用户是否拥有该权限
   - 若权限名包含 `_own_peak` 或用户角色是峰级角色，则校验资源的 `peak_id` 是否在用户的 `peak_ids` 列表中
   - 全局角色（宗主、大长老、太上长老、荣誉长老）直接放行
3. **全协会资源**：无峰限制的资源（`peak_id IS NULL`），峰主无法操作

#### 5.2.1 全局角色判定（PermissionServiceImpl.isGlobalRoleUser）

`PermissionServiceImpl.isGlobalRoleUser()` 方法用于判定当前用户是否为全局角色用户，进而决定其在峰级数据范围内是"可查看所有峰"还是"仅可查看本峰"。

**判定逻辑**：

该方法同时检查以下两个条件，满足任一即判定为全局角色用户：

1. **角色名称匹配**：用户在 `user_roles` 表中关联的角色（通过 `roles` 表）名称属于全局角色集合：
   - `sect_master`（宗主）
   - `grand_elder`（大长老）
   - `supreme_elder`（太上长老）
   - `honor_elder`（荣誉长老）

2. **peak_id 为 NULL**：用户在 `user_roles` 表中存在 `peak_id IS NULL` 的角色关联记录（即未绑定具体峰的全局角色）。

> **注意**：`user_roles` 表结构未发生变化，仍保留 `peak_id` 字段。变更点在于查询逻辑——原先仅依据 `peak_id IS NULL` 判定，现新增对 `roles.name` 的检查，使显式拥有上述全局角色名的用户即使存在峰绑定记录，也被识别为全局角色用户。

**影响范围**：

| 场景 | 全局角色用户 | 峰级角色用户 |
|------|-------------|-------------|
| 查看财务 | 可查看所有峰财务 | 仅查看本峰财务 |
| 查看成员 | 可查看所有峰成员 | 仅查看本峰成员 |
| 查看任务 | 可查看所有峰任务 | 仅查看本峰任务 |
| 数据范围过滤 | 不按 `peak_id` 过滤 | 按 `peak_id` 过滤 |

#### 5.2.2 数据过滤双保险机制

为确保数据访问控制的可靠性，所有数据过滤逻辑在与 `isGlobalRoleUser()` 判定并列的位置，均增加了对应的 `view_all` 权限检查作为第二道保险。即全局访问条件为：`isGlobalRoleUser(userId) || hasPermission(userId, "<module>:view_all")`，二者满足其一即视为全局可访问，不再按 `peak_id` 过滤。

> **说明**：此为代码层面的数据访问控制变更，不涉及数据库 schema 改动。引入双保险的目的是确保大长老等全局角色即使在角色判定异常时，仍可通过 `view_all` 权限正常访问全量数据。

**各模块双保险权限对照**：

| 数据范围 | 第一道保险 | 第二道保险 | 说明 |
|----------|------------|------------|------|
| 成员/弟子数据 | `isGlobalRoleUser()` | `member:view_all` | 成员名册、弟子列表等数据过滤 |
| 任务数据 | `isGlobalRoleUser()` | `quest:view_all` | 任务列表、待审任务、任务详情等数据过滤 |
| 财务流水数据 | `isGlobalRoleUser()` | `finance:view_all` | 灵石流水、财务统计等数据过滤 |

### 5.3 角色层级保护

系统防止越权操作，用户不能修改等级高于自己的用户：

```sql
-- 获取当前用户的角色层级
SELECT MIN(r.level) as min_level
FROM user_roles ur
JOIN roles r ON ur.role_id = r.id
WHERE ur.user_id = :operatorId;

-- 修改用户角色时检查：目标用户的角色层级不能高于当前用户
```

### 5.4 权限数据迁移（PermissionMigrator）

`PermissionMigrator` 是一个在应用启动时自动执行权限数据迁移与对齐的组件（`@Order(2)`，在 `DataInitializer` 之后执行），负责将历史 RBAC 数据迁移至与 `rbac_init.sql` 一致的状态。

**职责**：

1. **权限重命名**：将历史权限标识迁移至新命名规范
2. **补全缺失权限**：插入 `rbac_init.sql` 中新增但数据库中尚未存在的权限定义
3. **角色-权限关联对齐**：根据最新权限定义重新授予各角色权限

**已修复项**：

| 修复项 | 说明 |
|--------|------|
| `peak_master` 角色引用修正 | 历史代码中引用的 `peak_master` 角色在 `rbac_init.sql` 中并不存在，已统一改为正确的峰主角色标识 `elder` |
| `grand_elder` 权限对齐 | 调整 `grand_elder`（大长老）的权限集合，使其与 `rbac_init.sql` 保持一致——拥有除 `system:admin` 外的全部权限（此前仅授予子集） |
| `affair:manage_event` 重命名修正 | 将历史权限 `affair:manage_event` 迁移至 `affair:manage_all_events`，以匹配 `rbac_init.sql` 中的权限命名 |
| `affair:manage_event` 迁移条目 | 新增 `affair:manage_event` → `affair:manage_all_events` 迁移条目，处理此前已被错误重命名为 `affair:manage_event` 的权限记录 |
| 移除重复授权块 | 删除 `elder` 角色权限授予中重复的代码块，避免重复写入导致的数据冗余 |

> **注意**：权限重命名采用幂等设计（`ON CONFLICT DO NOTHING`），重复执行不会产生副作用。迁移器在角色不存在或权限不存在时会跳过对应授权并记录日志，不会中断启动流程。

---

## 六、开发与维护指南

### 6.1 环境配置

**application.yml 数据库配置**：

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  sql:
    init:
      mode: never
```

**环境变量**：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| DB_HOST | 数据库主机 | localhost |
| DB_PORT | 数据库端口 | 5432 |
| DB_NAME | 数据库名 | sect_db |
| DB_USERNAME | 数据库用户名 | postgres |
| DB_PASSWORD | 数据库密码 | (空) |

### 6.2 添加新权限

**步骤1**：在 `rbac_init.sql` 中添加权限定义

```sql
INSERT INTO permissions (name, display_name, module, description) VALUES
    ('new_module:new_action', '新权限显示名', 'new_module', '权限描述')
ON CONFLICT (name) DO NOTHING;
```

**步骤2**：为需要该权限的角色添加关联

```sql
INSERT INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id FROM roles r, permissions p
WHERE r.name = 'sect_master' AND p.name = 'new_module:new_action'
ON CONFLICT DO NOTHING;
```

**步骤3**：在 Controller 方法上添加权限注解

```java
@PostMapping("/new-action")
@RequiresPermission("new_module:new_action")
public Result<Void> newAction() {
    // ...
}
```

### 6.3 添加新角色

**步骤1**：在 `rbac_init.sql` 中添加角色定义

```sql
INSERT INTO roles (name, display_name, description, level, is_system) VALUES
    ('new_role', '新角色显示名', '角色描述', 2, 0)
ON CONFLICT (name) DO NOTHING;
```

**步骤2**：为新角色分配权限

```sql
INSERT INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id FROM roles r, permissions p
WHERE r.name = 'new_role' AND p.name IN ('permission1', 'permission2')
ON CONFLICT DO NOTHING;
```

### 6.4 常见问题排查

**问题1：权限校验不生效**

```
检查步骤：
1. 确认数据库中是否存在该权限记录
2. 确认角色-权限关联是否正确
3. 确认用户-角色关联是否正确
4. 确认注解拼写是否正确（区分大小写）
5. 确认用户是否拥有全局角色（会绕过峰限制）
```

**问题2：任务状态无法更新**

```
检查步骤：
1. 确认当前用户是否有对应权限（quest:review, quest:submit等）
2. 确认任务当前状态是否允许该操作
3. 确认任务的publisher_id/completer_id是否正确
4. 查看audit_logs是否有相关操作记录和错误信息
```

**问题3：登录失败（Redis锁定）**

```
检查步骤：
1. 确认Redis服务是否正常运行
2. 查看Redis中是否有登录失败记录（key格式：login_fail:{username}）
3. 等待15分钟自动解锁，或手动清除Redis中的失败记录
```

### 6.5 性能优化建议

**索引优化**：
- 已在初始化脚本中添加常用索引
- 根据实际查询场景添加额外索引

**查询优化**：
- 使用 `EXPLAIN ANALYZE` 分析慢查询
- 避免全表扫描，确保查询条件命中索引
- 使用分页查询，避免一次性加载大量数据

**连接池配置**：
- 根据服务器配置调整连接池大小
- 设置合理的连接超时和最大等待时间

---

## 七、安全注意事项

1. **密码安全**：所有密码使用 BCrypt 加密存储，禁止明文存储
2. **SQL注入防护**：使用 MyBatis-Plus 的参数绑定功能，禁止拼接SQL
3. **权限校验**：所有敏感操作必须经过权限校验，禁止直接访问数据库
4. **审计日志**：所有敏感操作自动记录，便于安全追溯
5. **备份加密**：数据库备份文件应加密存储，定期更换密码
6. **最小权限原则**：数据库用户只授予必要的权限，避免使用超级用户运行应用
7. **生产环境配置**：修改默认密码（admin/admin123），配置强JWT密钥

---

## 八、附录

### 8.1 脚本文件路径

```
src/main/resources/sql/
├── Users.sql              # 用户表结构
├── rbac_init.sql          # RBAC表结构和初始化数据
└── task_and_sect_init.sql # 任务表和弟子表结构
```

### 8.2 数据库初始化脚本执行顺序

```
1. Users.sql              → 创建users表
2. rbac_init.sql          → 创建peaks, permissions, roles, role_permissions, 
                            user_roles, audit_logs表，并初始化数据
3. task_and_sect_init.sql → 创建disciples, tasks表
```

### 8.3 Spring Boot 启动时自动初始化

系统通过两个组件在应用启动时自动执行初始化：

#### 1. DatabaseAutoInitializer（数据库级别初始化）

**执行顺序**：`@Order(0)`（最先执行）

**功能**：
```
检测数据库是否存在
   ├─ 不存在 → 自动创建数据库
   │             ├─ 配置字符集：UTF-8
   │             ├─ 配置排序规则：zh_CN.UTF-8
   │             └─ 若不支持，降级为默认配置
   └─ 存在 → 检测表结构完整性
               ├─ 完整 → 跳过初始化
               └─ 不完整 → 执行初始化脚本
```

**初始化内容**：
- 数据库创建（若不存在）
- 表结构初始化（按顺序执行 3 个 SQL 脚本）
- 基础数据填充（峰、权限、角色等）

**所需权限**：
- `CREATEDB`：创建数据库
- `CREATE`：创建表结构
- `INSERT`：插入初始数据
- `INDEX`：创建索引

#### 2. DataInitializer（应用级别初始化）

**执行顺序**：`@Order(1)`（在数据库初始化后执行）

**功能**：
```
检查管理员账号是否存在
   ├─ 存在 → 跳过创建
   └─ 不存在 → 创建默认管理员
                 ├─ 用户名：admin
                 ├─ 密码：admin123
                 └─ 角色：宗主（sect_master）
```

#### 初始化流程图

```
┌─────────────────────────────────────────────────────┐
│            Spring Boot 应用启动                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│   DatabaseAutoInitializer (@Order(0))               │
│   ─────────────────────────────────────              │
│   1. 检测数据库是否存在                              │
│   2. 不存在则自动创建                                │
│   3. 检测表结构完整性                                │
│   4. 不完整则执行初始化脚本                          │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│   DataInitializer (@Order(1))                       │
│   ─────────────────────────────────────              │
│   1. 检查管理员账号是否存在                          │
│   2. 不存在则创建默认管理员                          │
│   3. 自动分配宗主角色                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│            应用启动完成                              │
│   ─────────────────────────────────────              │
│   ✓ 数据库已就绪                                    │
│   ✓ 表结构已创建                                    │
│   ✓ 初始数据已填充                                  │
│   ✓ 管理员账号已创建                                │
└─────────────────────────────────────────────────────┘
```

**首次登录后请立即修改管理员密码！**

### 8.4 数据一致性校验

系统在以下场景保证数据一致性：

| 场景 | 处理方式 |
|------|----------|
| 添加弟子 | 自动创建用户表记录（默认密码123456）→ 创建弟子表记录 → 创建user_roles关联记录 |
| 修改弟子角色 | 同步更新user_roles关联记录 |
| 删除弟子 | 同步删除user_roles关联记录及弟子表记录，保留用户表记录 |
| 移动弟子门派 | 同步更新user_roles.peak_id和disciples.peak |
| 删除峰 | user_roles.peak_id置空（ON DELETE SET NULL）；峰管理员降级为普通成员；disciples.peak清空（详见4.4节峰删除级联规则） |
| 删除角色 | 级联删除role_permissions和user_roles（ON DELETE CASCADE） |
| 删除权限 | 级联删除role_permissions（ON DELETE CASCADE） |
